dist
node_modules
.env
.env.*
!pinhead-react/.env.example
.playwright-mcp/
*.png
!pinhead-react/e2e/**/*.png
!pinhead-react/public/icon-*.png
.DS_Store
test-results/

# Runtime-данные хуков (не версионируется).
# .agents/skills и skills-lock.json — НАОБОРОТ в репозитории: туда
# `npx skills add` кладёт внешние скилы, и на них из .claude/skills симлинки.
.claude-flow/
.swarm/
.claude/helpers/*.db
.hive-mind/
memory.db
