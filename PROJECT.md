# Pinhead Order Studio — Проект

## История

### ERA 1 — Vanilla HTML (до марта 2026)
Весь проект в одном файле `pinhead-order-studio-v1_7__5_.html` (~9 500 строк).
Supabase через CDN. Финальная версия v1.7 — полностью функциональна для внутреннего использования.

Ушли от vanilla потому что: файл неподдерживаем, нет тестов, нет типов, невозможно масштабировать.

### ERA 2 — React Migration (февраль–март 2026)
Полная миграция в React 19 + Zustand. 138 коммитов. Воспроизведён весь функционал v1.7.

Ключевая сессия — 20 марта 2026 (56 коммитов): ревизия по Design Guidebook, 5 критических багов, 65 тестов на pricing.

### ERA 3 — Стабилизация + CRM/ERP (апрель 2026)
Сессии 1-5: баги, качество, UX, аудит UI, security hardening. Стратегический разворот к CRM/ERP интеграции.

---

## Changelog

Журнал сессий живёт в `docs/changelog/` — по файлу на месяц, указатель
`docs/changelog/INDEX.md` (перенос 26.09, сессия 69: здесь он занимал
3865 строк из 3956). Запись новой сессии — в файл текущего месяца.

## Статистика (26.09.2026, сессия 69)

Пересчитано по живым источникам; прежняя таблица была от 10.04 и держала
«God-компоненты (>500 строк): 0» при 25 таких модулях.

| Метрика | Значение |
|---------|----------|
| Тесты (unit) | 4795 в 297 файлах (Vitest); полный прогон 109 с (был 132) |
| Тесты (E2E) | 24 спеки Playwright + визуальные эталоны |
| TypeScript | `strict`, 0 ошибок; компоненты `.jsx` — «новые `.tsx`, старые по касанию» |
| Критический путь оболочки | 206 649 Б gzip (JS+CSS), бюджет в CI |
| База (`pinhead-os-v2`) | 55 таблиц `public`, RLS на всех, 178 политик, 107 функций |
| Журнал миграций | 296 записей = `APPLIED.json` (сверка md5 с продом) |
| Стражи UPDATE | 9; семь сравнивают снимки строки (исключения), `erp_material_guard` — узкий по решению 10.08, `erp_dev_package_guard` — гейт полноты пакета |
| Модули > 500 строк | 25 — ратчет `src/fileSizeRatchet.test.ts`, только вниз (за сессию 69 опущены шесть потолков) |
| Storage | `erp-attachments` 401 МБ / 335 объектов, `sku-photos`; ночная копия в приватный `storage-backup` |

---

## Roadmap

> Переписан 24.09 (сессия 68) под факт. Прежняя редакция была апрельской:
> «Фаза 2 — планирование производства (следующая)» при сделанных `/plan`,
> `/gantt`, `/load`, и таблица без единого признака ERP.

### Сделано (ERP, июль–сентябрь 2026)
- Производство: заказы и позиции с ТЗ, маршрут этапов по цехам, очереди цехов,
  канбан, задания, финальный ОТК, подряд, экспериментальный цех (разработка
  образцов), склад (приёмка, маркировка, упаковка, отгрузка частями), закупка
  (варианты поставщиков, приёмка частями, рулоны и их вес, остатки полотна).
- Планирование: недельный план `/plan` — РУЧНОЙ по решению заказчика,
  загрузка цехов `/load`, Гант `/gantt`; мощность — в штуках за месяц.
- Права: матрица ролей на клиенте и на сервере (`erp_has_permission`),
  девять стражей UPDATE, RLS на всех таблицах, аварийный режим.
- ТЗ в PDF у позиции и заказа, каталог моделей SKU, чат внутри сделки
  с уведомлениями и реакциями, аналитика и экономика позиции.
- Эксплуатация: CI (unit, e2e, визуальные эталоны, бюджет бандла), защита
  `main`, журнал миграций со сверкой с продом, сбор ошибок интерфейса
  (`erp_client_errors`), ночная копия Storage.
- Order Studio (за флагом): визард, SKU-редактор, аналитика; номер заказа
  из последовательности (`generate_order_number`, 24.09).

### Следующая очередь (решение заказчика — `CLAUDE.md`)
- [ ] Мост «ТЗ → авто-создание производственного заказа».
- [ ] Генерация ТЗ-PDF в Order Studio.

### Интеграции — не начаты
- [ ] Bitrix24 sync (edge-функции bitrix-inbound / bitrix-sync, таблицы связей) — ждёт доступа.
- [ ] 1С: выгрузка + HTTP-сервис на стороне 1С (нужен 1С-разработчик).

### Техдолг (обзор 24.09 — `docs/2026-09-24-project-review.md`)
- [ ] 25 модулей > 500 строк: ратчет держит их от роста; резать по касанию,
      начиная с `ItemBlock.jsx` (941 строка, без тестов).
- [ ] Порог покрытия (`vitest --coverage`) — нужна зависимость `@vitest/coverage-v8`.
- [ ] `erp_materials`: закупочные поля открыты любому участнику на сервере
      (решение 10.08) при закрытом правами экране закупки — решение владельца.
- [ ] Внешняя копия Storage — сейчас копия в том же проекте Supabase.
- [ ] Превью-окружение Vercel без `VITE_SUPABASE_*`.
- [ ] 84 индекса без чтений (advisor 26.09) — пересмотреть в октябре по статистике; два префиксно-избыточных сняты 26.09.
- [ ] Типизация стора: 78 мест `Record<string, unknown>` в `src/store`.
- [ ] Предупреждения тестов: canvas/Chart.js в jsdom.
- [ ] Консолидация хранения prices (`app_config` + `catalog_config`) — не сверялось.
- [ ] Клиент Supabase без `<Database>`: 93 ошибки при включении, настоящих две —
      вместо типов стоит сторож имён `schemaNames.test.ts`.

### Отклонено / отложено
- Покупательский портал /order — приоритет сменился на CRM/ERP.
- Автоматическое составление плана производства — план ставит человек
  (решение заказчика); система показывает отклонение.
- Мощности цехов по дням (`capacity_per_day`) — удалены осознанно,
  загрузка выражается в штуках.
