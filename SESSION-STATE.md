# SESSION-STATE — память проекта PinheadOS

> Живой документ: обновляется в конце КАЖДОЙ сессии (правило в CLAUDE.md).
> Здесь — текущее состояние и решения ДВУХ последних сессий плюс открытые долги.
> История старше — `docs/sessions/` (по файлу на сессию, указатель
> `docs/sessions/INDEX.md`), changelog — `PROJECT.md`. Сторож
> `src/sessionsIndex.test.ts` не даёт истории снова копиться здесь.

## Состояние на 2026-09-24 (сессия 68) — хвосты обзора: пароли, номер заказа, ветки, бэкап

Ветка `claude/zen-pasteur-0u4l6x`. Владелец дал четыре пункта из «порядка
работ» обзора 67; правила — `docs/rules/pravila-hvostov-obzora-24-09-sessiya-68.md`.

### Что сделано

- ~~**Leaked password protection включена.**~~ **НЕ ПОДТВЕРДИЛОСЬ** (сверка
  26.09, сессия 69): advisor `auth_leaked_password_protection` снова в списке
  `get_advisors security`. Либо запись через Management API не закрепилась,
  либо повторное чтение сессии 68 смотрело не туда. Тумблер — у владельца:
  Dashboard → Authentication → Password security → «Leaked password
  protection». `SUPABASE_ACCESS_TOKEN` в окружении сессии лежит в угловых
  скобках `<…>` — переменную стоит поправить.
- **`generate_order_number` заведена** — миграция
  `20260924222445_order_studio_generate_order_number`, применена до мержа
  (журнал 291 = `APPLIED.json` 291, md5 `9dcf969c4921a1bab601903cbe442776`).
  Читает ту же `order_number_seq`, что и умолчание `orders.order_number`,
  и умолчание переведено на неё — формат в одном месте. `lpad` больше
  не режет номер после 9999 (прежнее умолчание дало бы `PH-1000` 10000-му
  заказу). `security invoker`; исполнение — `authenticated` и `service_role`,
  не anon. Проверено на бою без расхода номера: REST под anon → 42501
  (функция в кэше схемы), последовательность осталась на 18 — первый
  настоящий номер `PH-0019`. Сторож `store/orderNumber.test.ts` проверен
  четырьмя мутациями; имя ушло из `KNOWN_MISSING`. Смержено #178.
- **Бэкап Storage переделан** (владелец: «делай сам всё, репо закрывай»).
  Вместо архива в артефактах GitHub — СЕРВЕРНАЯ копия (`copy` с
  `destinationBucket`) в приватный бакет `storage-backup` того же проекта,
  только новое, без путей в логе. Бакет — миграция `20260924230014`
  (журнал 292 = снимок, md5 `e43b5df3…`), `public = false`, политик нет;
  снаружи проверено: анонимный листинг — `[]`, публичная ссылка — 400.
  Логика прогнана на макете Storage (335 объектов: копия всех → повтор 0 →
  изменённый сохраняет прежнюю версию → удалённый из рабочего остаётся).
  Сторож `storageBackup.test.ts` — шесть мутаций. За ночь уходит прирост
  (~20–30 МБ), а не 398 МБ; квоты артефактов приватного репозитория это
  больше не касается.
- **npm-скрипт `storage:gc` получил третий носитель.** Сессия 67 вписала
  `erp_sku_card_files` только в edge-функцию, а сторож читал только её:
  `npm run storage:gc -- --apply` удалил бы техпакет модели, как только
  у файла пропало бы исходное вложение (на 24.09 таких 0). Сторож теперь
  читает обе реализации.

### Что не сделано и почему

- **Ветки.** Пуш тегов `archive/*` → **403 политикой прокси**; не обходили,
  удаление не начинали: теги — страховка ровно тех 21 ветки, у которых
  нет PR. Классификация перепроверена на ПОЛНОЙ истории (облачный клон
  неглубокий, и первая сверка насчитала 39 «несмерженных» вместо 23):
  42 смержены (23 — предки `main`, 19 — голова совпадает с головой
  последнего смерженного PR), 4 с открытым PR, 21 под тег (головы совпали
  с отчётом). В отчёте 67 стояло «63 смержены» — исправлено на 42.
  Команда из отчёта годна для локального запуска; сверх списка можно
  удалить `claude/opus-55-project-review-k6rbpp` (#177 смержен).
- **Storage backup — секреты ЗАДАНЫ.** Из сессии запуск не прошёл:
  `run_workflow` → **403 Resource not accessible by integration** (у GitHub-
  интеграции нет `actions: write`). Прогон #58 запустил владелец
  (24.09 22:41 UTC), и шаг «Проверить секреты» впервые из 58 прогонов зелёный.

  **⚠️ Бэкап публикует файлы.** Репозиторий ПУБЛИЧНЫЙ (`private: false`),
  оба бакета — `public`. Workflow печатает в лог путь каждого объекта, а путь
  в публичном бакете и есть ссылка на файл, и выкладывает архив всех файлов
  (335 объектов, ~401 МБ) в артефакты на 30 дней — их скачивает любой вошедший
  на GitHub. Владельцу рекомендовано отменить или удалить прогон #58 (артефакт
  и логи) и не запускать workflow в нынешнем виде: либо приватный репозиторий,
  либо шифрованный архив без печати путей, либо приватное хранилище.

### Стражи на исключениях — ИСПРАВЛЕНО (миграция `20260924231233`)

Четыре стража — вложения, уведомления, план, этапы — сравнивают снимки
строки (`to_jsonb(new)` минус разрешённое) вместо поимённого списка;
применено до мержа (журнал 293). Проба на бою в откатываемой транзакции:
`message_id` вложения, `created_at` этапа, заголовок уведомления — 42501;
законные дороги открыты (временный QA-менеджер переложил файл между
папками, QA-рабочий своего цеха поправил заметку этапа). Сторожа переписаны
на конструкцию — общий разборщик `snapshotExclusions` в `migrations.testutil`;
шесть мутаций. Ниже — как это было найдено.

**`erp_attachment_guard` не охранял `message_id`**: колонку дописала миграция
чата 14.09, после стража. Держатель `files.manage` (по живой матрице —
`designer`, `manager`, `dispatcher`, `production_head`, `director`) может отправить сообщение в любой чат, перепривязать к нему ЛЮБОЕ вложение
(`update … set message_id = <своё>` — политика UPDATE стоит на
`files.manage`, страж пропускает) и удалить своё сообщение через
`erp_chat_delete`: она `security definer` и удаляет вложения по `message_id`
мимо DELETE-политики. Так уходят `stage_result`, `dev_*`, `preview`, `tech` —
то, что политика отдаёт только админу или `experimental.manage`; объект
в бакете становится ничьим и уходит с `storage-gc`. Перепроверено по всем
статическим слоям: UPDATE у `authenticated` — на всю таблицу, включая
`message_id` (колоночных ограничений нет); RLS — `files.manage`; других
UPDATE-триггеров нет; CHECK, связывающего `message_id` с видом `chat`, нет;
`erp_chat_send` и `erp_chat_delete` исполняемы. Под ударом были 24 строки
защищённых видов.

Рядом, тот же класс (п. 13 обзора): у `erp_item_stages` не охранялись
`id`/`created_at` — любой участник ERP, смена `id` рвала граф `depends_on`
(массив без FK); теперь они неизменны для всех, кроме service_role.
У `erp_calendar_slots` — те же две колонки для `plan.fact`, теперь под `plan.manage`.
`erp_material_guard` — **не тот класс**: закупочные поля открыты записанным
решением 10.08 («ведение закупки не гейтится»), страж сторожит узкое
подмножество приёмки, и перевод на исключения запер бы закупщика.

### Третья порция: ратчет, история, roadmap, индекс

- **Ратчет размера файлов** (`src/fileSizeRatchet.test.ts`, п. 11): новый модуль
  ≤ 500 строк, 25 крупных записаны со своими размерами и расти не могут;
  похудевший обязан опустить потолок. Три мутации (рост, похудание, новый файл).
- **История сессий переехала** (п. 5): 79 разделов — в `docs/sessions/` по файлу
  на раздел, дословно (склейка сверена с исходником побайтно); здесь — две
  последние сессии. Сторож `src/sessionsIndex.test.ts`.
- **Roadmap и статистика `PROJECT.md` переписаны под факт**: прежние были
  апрельскими («планирование — следующая фаза», «God-компонентов: 0»).
- **Индекс на `orders.erp_order_id`** (п. 14) — миграция `20260924233145`,
  журнал 294; advisor `unindexed_foreign_keys` пуст.
- **Красный `Supabase Preview` на PR #180 — гонка двух прогонов, а не дефект.**
  Два пуша пришли, пока превью-ветка ещё создавалась. Один прогон упал
  на `create policy` миграции 23.08, последняя проверка того же коммита
  зелёная, журнал превью-базы полный (264 из 264). Порядок разбора — в правилах
  сессии 68.

### Страж карточки SKU и ловушка `set null` — ИСПРАВЛЕНО (миграция `20260924235701`)

`erp_sku_card_guard` сравнивает снимки строки — седьмой из девяти стражей.
Открыты были `id`, `experimental_id`, `source_item_id`, `created_by`,
`created_at`: держатель `sku.edit` (технолог, дизайнер) перепривязывал
карточку к чужой разработке. `experimental_id` — под `sku.publish`, потому что
`erp_sku_from_dev` меняет его через `on conflict (code) do update`; остальное
неизменно. Применено до мержа (журнал 295, отпечаток сверен с продом).

Найдено по дороге: **ссылка `on delete set null` у таблицы со стражем.**
Удаляя родителя, Postgres обнуляет ссылку UPDATE-ом, и страж видит его
с `auth.uid()` удаляющего. Контрольная проба: без отдельной ветки удаление
позиции, из которой завели модель, падало 42501 даже у админа. Ветка
«родитель удалён» узнаёт обнуление по факту (ссылка ушла в NULL, родителя
нет). Проба на бою в откатываемой транзакции: технолог — правка названия
проходит, привязка, источник, автор, `id`, `created_at`, выпуск — 42501;
начальник производства — привязка, путь `erp_sku_from_dev` и код проходят,
источник и автор — 42501; удаление разработки и позиции технологом проходит.
Сторожа `skuCardGuard.test.ts` и общий для всех стражей `guardSetNull.test.ts`
(ссылки берёт из `FK_SNAPSHOT.md`), семь мутаций.

### Next steps

- Мост «ТЗ → производство» (следующая очередь): `erp_orders.tz_order_id` →
  `orders` — `on delete set null`, а `erp_order_guard` пускает смену колонки
  только с `order.manage`. Заказ ТЗ удаляют admin, director, rop и
  менеджер-автор; их роли по умолчанию право имеют, но rop или менеджер
  с цеховой должностью без `order.manage` связанный заказ ТЗ удалить
  не сможет. Сегодня связанных 0; записано исключением в `guardSetNull.test.ts`.
- Решение владельца по `erp_materials`: закупочные поля открыты любому
  участнику на сервере (решение 10.08), а экран закупки с тех пор закрыт
  правами `material.receive`/`order.manage` — сервер мягче интерфейса.
- За владельцем: **сделать репозиторий приватным** (Settings → General →
  Danger Zone → Change visibility; инструмента у сессии нет) и удалить
  артефакт прогона #58 (398 МБ) после первого прогона новой копии; удаление
  веток (команда в отчёте 67); гейт деплоя Vercel по CI. После перевода
  в приватный — следить за минутами Actions: CI ~13 минут раннера на пуш.
  Вендорные файлы ruflo (`.claude/helpers/`, `.claude-flow/`) уже удалены
  сессией 67 — строка «`git rm -r` за владельцем» в отчёте устарела.

---

## Состояние на 2026-09-24 (сессия 67) — обзор проекта целиком

Ветка `claude/opus-55-project-review-k6rbpp`. Отчёт —
`docs/2026-09-24-project-review.md`: пятнадцать находок по трём приоритетам,
у каждой — путь или замер с живого источника (advisors и журнал миграций
прода, GitHub Actions, список веток).

### Что сделано в коде

- **Техпакет карточки модели больше не теряется.** `erp_sku_card_files.file_path`
  — снимок пути вложения разработки без копии в бакете, и объект сносили
  три клиентских удаления (заказ, файл разработки, вложение заказа) плюс
  уборщик `storage-gc`, который знал два носителя из трёх. Теперь: третий
  носитель в `REFERENCES`, `freeOfSkuCards` в `store/shared.ts` перед каждым
  `remove` (ошибка чтения = ничего не убирать), сторож
  `erp/utils/storageGc.test.ts` выводит носители ИЗ МИГРАЦИЙ. Обе половины
  проверены мутацией.
- **claude-flow выключен:** сервер снят из `.mcp.json`, хуки и `CLAUDE_FLOW_*`
  из `settings.json`, allow-list переписан (запись в бой —
  `apply_migration`/`execute_sql` — больше не разрешена заранее).
- **Память поправлена:** запись «миграции 24.09 не применены» была ложной
  (применены до мержа #176, журнал прода это подтверждает); ссылка
  в `CLAUDE.md` на несуществующий аудит заменена.

### Приёмник ошибок интерфейса — работает

`erp_client_errors` (миграция `20260924205222`, журнал 290 = 290) +
`lib/errorReport.ts` пишет туда отчёт, когда `VITE_ERROR_REPORT_URL` пуст,
+ вкладка админки «Ошибки» (`screens/admin/ClientErrorsTab.jsx`, право
`staff.invite` — то же, что у политики чтения; совпадение сторожит тест).
На бою проверено: чужой `user_id` отклоняется, без права строки не видны,
`anon` прав не имеет. Ошибки до входа не пишутся — осознанно.

### Что требует действия владельца

0. ~~**`generate_order_number`** — Order Studio зовёт функцию, которой нет
   на бою~~ — **ЗАВЕДЕНА** в сессии 68 (`20260924222445`, решение владельца);
   имя ушло из `KNOWN_MISSING`.
1. ~~**Секреты `Storage backup`**~~ — **ЗАДАНЫ** (прогон #58 владельца,
   24.09 22:41 UTC: шаг секретов зелёный). Но см. сессию 68: репозиторий
   публичный, и лог с путями и архив бакета видны всем.
2. ~~**Защита `main`** — `protected: false`~~ — **ВКЛЮЧЕНА** владельцем
   (сверка сессии 68: `protected: true`, PR до зелёного CI — `blocked`).
   Гейт деплоя Vercel по CI — не сверялся.
3. **Leaked password protection** — сессия 68 записала «включена», но
   advisor 26.09 показывает выключено (см. сессию 69). Тумблер за владельцем.
4. **Удалить старые ветки на GitHub** — сессия не может: git-прокси пускает
   пуш только в рабочую ветку. Список и команда — в отчёте сессии 67
   (`docs/2026-09-24-project-review.md`, «Ветки»). Вендорные файлы ruflo
   удалены из репозитория в этой же сессии.

### Мусор убран

Удалено (каждое проверено: ссылок нет либо файл не работает): корневые
`package.json`/`package-lock.json` (`agentation ^2.3.3`, ни разу
не установлен), `skills-lock.json`, `scripts/seed-catalog.js` +
`generate-seed-sql.js` + `seed-catalog.sql` (падали на загрузке),
`docs/plans/backlog-progress.md`, `docs/superpowers/specs/2026-04-11-sku-tz-hub-design.md`,
`docs/load-test-report.html`, `pinhead-react/tests/E2E-TEST-PLAN.md`,
вендорные `.claude/helpers/` (44 файла). Ссылки поправлены.

Мёртвого кода в `src/` НЕТ: все 415 модулей кем-то импортируются, все
зависимости и ассеты используются, снимков-сирот нет. Пять проектных скилов
переписаны: учили строить отменённый портал `/order`.

### Решения, которые стоит помнить

**Носители ключа бакета выводятся из схемы, а не перечисляются.** Список
в уборщике отстал от схемы на девять дней ровно так же, как поимённый
список колонок в страже заказа. Сторож, который проверяет известные имена,
на такое отставание не реагирует; сторож, который читает миграции, — да.

**Ошибка проверки «можно ли удалять» = не удалять.** Удаление файла
необратимо, сирота стоит копейки и убирается уборщиком. Неверный ответ
опаснее отсутствия ответа.

**Документ-память — не источник истины о базе.** Три утверждения разведки
не прошли сверку с живым источником: состояние миграций, счёт падений
бэкапа, статус проверки Preview. Все три повторяли записи из `SESSION-STATE.md`.

**Сильной модели нужен чистый контекст, а не роутер.** Надстройка,
которая не поднималась две сессии подряд и отвечала «coder, 30%» на каждый
запрос, стоила хуками на каждом событии и шумом ±3502 строк в истории.

### Next steps

- ~~Наблюдаемость без зависимости: `erp_client_errors` + плитка в админке~~ —
  сделано в этой же сессии (вкладка «Ошибки»); строка осталась от черновика.
- Удалить смерженные ветки `claude/*`, включить auto-delete — за владельцем:
  из сессии пуш тегов и удаление отвечают 403 (проверено в сессии 68).
- ~~`SESSION-STATE.md`: оставить текущий блок, историю — в `docs/sessions/`;
  Roadmap в `PROJECT.md` переписать под факт~~ — сделано в сессии 68.
- `createClient<Database>()` — подключить сгенерированные типы к клиенту.

---

## История

Разделы сессий 66 и старше — `docs/sessions/INDEX.md` (79 файлов, перенос
дословный, сессия 68). В конце сессии старший из разделов здесь переезжает
туда же, чтобы в этом файле оставались две последние сессии.
