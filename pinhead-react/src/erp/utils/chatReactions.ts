/**
 * НАБОР РЕАКЦИЙ (вторая очередь чата, документ 20.09, п. 4).
 *
 * Живёт в данных клиента, а не в CHECK таблицы: список «какие лица
 * предлагать» меняется от привычек людей, и CHECK на него означал бы
 * миграцию на каждое новое. Сервер ограничивает только ДЛИНУ — реакция
 * не должна оказаться сообщением.
 *
 * Отдельным модулем, а не константой в компоненте: файл, который экспортирует
 * и компонент, и константу, ломает горячую перезагрузку (`react-refresh`),
 * и линтер раздела на это ругается справедливо.
 */
export const REACTION_CHOICES = ['👍', '🔥', '✅', '❤️', '😀', '👀'] as const;
