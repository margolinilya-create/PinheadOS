/**
 * «Профиль не привязан к участку» — одна формулировка на все экраны цеха.
 *
 * До 22.08 роль участка без привязки к цеху пропускалась в ЛЮБОЙ цех
 * (`canActInDept` возвращала true при пустом `department_id`). Гейт закрыли,
 * и теперь такой человек видит очередь без единой кнопки действия. Молча это
 * читается как поломка сайта, а не как незаконченное заведение сотрудника —
 * поэтому отказ обязан назвать себя.
 *
 * Компонент, а не строка в каждом экране: текст объясняет ПРАВИЛО, и два
 * разных его пересказа в очереди и на странице задания разошлись бы в первую
 * же правку. Показывать — по `useErpAccess().needsDeptBinding`.
 */

import styles from '../erp.module.css';

export default function DeptBindingNotice() {
  return (
    <div className={styles.warnBox} role="status">
      Ваш профиль не привязан к участку, поэтому действия по заданиям закрыты:
      взять в работу, внести результат и сообщить о проблеме нельзя. Очередь
      видна на чтение. Привязку ставит администратор в разделе «Сотрудники».
    </div>
  );
}
