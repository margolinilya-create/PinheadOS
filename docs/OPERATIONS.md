# Эксплуатация PinheadOS

> Что делать, когда что-то сломалось. Заводится по итогам аудита 29.07.2026
> (`docs/2026-07-29-full-audit.md`, находка C9: runbook отсутствовал).
> Держать коротким — длинный runbook не читают в инциденте.

## Координаты

| Что | Где |
|---|---|
| Прод-фронт | https://pinhead-os.vercel.app · проект Vercel `pinhead-os` |
| Прод-БД | Supabase `pinhead-os-v2`, ref `glhwbktsokphgksdvcxj`, регион eu-west-1, план Pro |
| Репозиторий | `margolinilya-create/PinheadOS`, прод собирается из `main` |
| Бакеты | `erp-attachments` (ТЗ, вложения), `sku-photos` (фото моделей) — оба public-read |

## Откатить релиз (30 секунд)

Vercel → проект → Deployments → выбрать предыдущий production-деплой с
`isRollbackCandidate: true` → **Instant Rollback**.

**Важно:** откат возвращает ТОЛЬКО фронтенд. Если релиз ехал вместе с миграцией БД,
старый фронт встретит новую схему. Поэтому правило релиза: миграции только аддитивные
(новая колонка nullable, новая функция рядом со старой), удаление колонок и смена
сигнатур RPC — отдельным релизом через неделю после того, как фронт перестал их
использовать. Тогда фронт-роллбэк остаётся достаточным откатом.

## Восстановить БД

1. Supabase Dashboard → Database → Backups.
2. План Pro даёт ежедневные бэкапы с хранением 7 дней. **PITR — платный аддон**;
   на 29.07.2026 признаков его включения нет — проверьте перед тем, как обещать
   точку восстановления точнее суток.
3. Restore in place: база маленькая (147 заказов / 443 этапа), занимает 20–40 минут.
   `project_ref` и URL не меняются, фронт переподключается сам.

**Что при этом теряется:**

- **до 24 часов работы цехов**, если PITR выключен: перемещения этапов, `qty_done`,
  брак, приёмка материалов. Восстанавливается только опросом людей;
- **файлы Storage бэкапы Supabase не покрывают** — только Postgres; версионирования
  у бакетов нет, а код удаляет через `remove()` (hard delete). С 24.09 (сессия 68)
  их страхует ночная копия в приватный бакет `storage-backup` ТОГО ЖЕ проекта —
  см. «Восстановить файл Storage» ниже. Потерю самого проекта она не покрывает:
  внешней копии нет;
- **edge-функция `domain-events-dispatcher`** — её исходника нет в репозитории,
  существует в единственном экземпляре на серверах Supabase.

## Восстановить файл Storage

Workflow «Storage backup» (`.github/workflows/storage-backup.yml`, 02:30 UTC)
каждую ночь серверно копирует НОВЫЕ объекты `erp-attachments` и `sku-photos`
в приватный бакет `storage-backup` под префиксом имени исходного бакета.
Из копии ничего не удаляется; если объект с тем же ключом сменил размер,
прежняя версия лежит рядом как `<ключ>~<время>`.

1. Supabase → Storage → `storage-backup` → `erp-attachments/<ключ>` (ключ —
   `file_path` из строки вложения, ТЗ или карточки модели).
2. Скачать и загрузить по тому же ключу в `erp-attachments` — либо серверно:
   `storage.from('storage-backup').copy('erp-attachments/<ключ>', '<ключ>',
   { destinationBucket: 'erp-attachments' })` с ключом `service_role`.
3. Строка-носитель, если её удалили вместе с файлом, восстанавливается из бэкапа
   БД — копия Storage возвращает только сам файл.

Бакет приватный: политик у него нет, читать может только `service_role`
(сторож — `src/erp/utils/storageBackup.test.ts`).

## Развернуть окружение с нуля

**Состояние на 29.07.2026: работает, подтверждено реальным реплеем.**

Превью-ветка Supabase на PR #132 (`cbqvuagqtmcffutikuzu`) собралась из репозитория
с нуля и совпала с продом по всем четырём срезам:

| Срез | Превью | Прод |
|---|---|---|
| таблиц `public` | 30 | 30 |
| функций `public` | 14 | 14 |
| политик RLS | 109 | 109 |
| бакетов | 2 | 2 |

Сверялись не счётчики, а md5 от отсортированных списков: имён таблиц; имён функций;
`таблица.политика:команда`; `таблица.колонка:тип:nullable`. **Все четыре хеша идентичны.**
Проверять стоит именно так — совпадение количеств само по себе ничего не доказывает.

Было: в проде 60 применённых миграций против 45 файлов в репозитории — 18 применялись
напрямую через MCP и файлов не имели. Реплей падал на
`20260727200000_erp_material_suppliers_rls`, потому что функции `erp_is_member()`
в схеме нет: во всём репозитории она только использовалась и нигде не создавалась.
Ветвление Supabase висело в `MIGRATIONS_FAILED` с 26.05.2026, то есть чек
`Supabase Preview` не проходил никогда (на PR без миграций он просто `skipped`).

Сделано:

- недостающие миграции выгружены **дословно** из `supabase_migrations.schema_migrations`
  (колонка `statements` хранит исходный SQL) и разложены файлами в порядке применения;
- добавлена `20260101000100_order_templates.sql` — таблица существовала в проде, но
  `create table` не было нигде, а `orderstudio_rls_perf` писал для неё политики;
- три легаси-файла (`20260415202746`, `20260515104159`, `20260515104329`) ссылались на
  объекты, снесённые `drop_legacy_erp_redesign_v2`, и на чистой базе падали бы с
  42P01/42883 — операции над ними обёрнуты в `to_regclass`/`exception`-guard'ы.
  Операции над пережившими объектами оставлены безусловными: без них свежее окружение
  получило бы `is_admin()` и `log_order_changes()`, вызываемые через `/rest/v1/rpc/`,
  и публичное чтение бакета `sku-photos`;
- сплошная проверка порядка по всем 53 файлам: неразрешённых ссылок на несозданные
  объекты не осталось.

**Что остаётся:**

- `supabase/config.toml` в репозитории нет, CLI не инициализирован, поэтому
  `supabase db reset` / `db push` локально по-прежнему недоступны — проверка реплея
  идёт только через превью-ветку на PR, трогающем миграции;
- восемь миграций `copy_*_from_shared` не переносились: это литеральные `INSERT`
  каталогов из другого Supabase-проекта (в `sku_catalog` зашиты URL фото чужого
  проекта), без `ON CONFLICT`. Их место — seed, а прежний `npm run seed` удалён 24.09 (сессия 67): он падал
  на загрузке (ссылался на удалённые экспорты) и писал ключи, которых
  `lib/catalogs.ts` не читает. Нужен seed — писать заново по `lib/catalogs.ts`;
- совпадение текста миграций не гарантирует совпадения итогового состояния схемы:
  гранты, владельцы, расширения и состав публикации `supabase_realtime` не сверялись.

## Как узнать, что сломалось

**Админка → вкладка «Ошибки»** (с 24.09, сессия 67). `lib/errorReport.ts`
ловит падения экранов (`ErrorBoundary`), ошибки скриптов (`window.onerror`),
необработанные промисы и отказы базы (`erpQuery`) и пишет их в
`erp_client_errors` от имени вошедшего: не больше 20 за сессию, без повторов.
Читает право `staff.invite`. Задан `VITE_ERROR_REPORT_URL` — отчёты уходят
туда (Sentry Store API, Logflare, вебхук), а не в таблицу.

Чего нет: ошибок до входа (вставка открыта только `authenticated`), Vercel Web
Analytics, алертов — вкладку надо открывать самому. Runtime Errors Vercel пусты
и будут пусты: приложение статическое, серверного рантайма нет.

Что есть: Supabase Logs (Postgres / PostgREST / Auth) — они отвечают на «упала ли
база», но не на «у раскройного цеха белый экран».

## Открытые эксплуатационные долги

Отмечать здесь по мере закрытия.

- [x] Схема воспроизводится из репозитория — подтверждено реплеем на PR #132
      (совпадение с продом по таблицам, функциям, политикам и колонкам)
- [ ] `supabase/config.toml` + CLI, чтобы `db reset` работал локально, а не только на PR
- [ ] Seed каталогов для чистого окружения: прежний `npm run seed` не работал и удалён 24.09 — писать заново по `lib/catalogs.ts`
- [x] Storage — ночная инкрементальная копия в приватный `storage-backup` (24.09, сессия 68)
- [ ] Внешняя копия Storage — вне проекта Supabase (сейчас копия лежит в том же проекте)
- [ ] PITR не подтверждён
- [x] Сбор ошибок интерфейса — `erp_client_errors` + вкладка «Ошибки» (24.09)
- [ ] Алерты и ошибки до входа (Sentry / вебхук — решение владельца)
- [x] Ветка `main` защищена, PR ждёт обязательных проверок (сверка 24.09, сессия 68)
- [ ] Превью-окружение не работает: `VITE_SUPABASE_*` заданы только для Production,
      поэтому превью-бандл падает на импорте `lib/supabase.ts` и открывается пустой страницей
- [ ] Edge-функция `domain-events-dispatcher` вне репозитория, `verify_jwt: false`
- [x] Runbook (этот файл)
- [x] Самоузаконивание визуальных эталонов (эталоны обновляются только явным диспатчем,
      коммит идёт в текущую ветку, `visual` стал гейтом)
- [x] `npm audit` в CI (гейт по critical)
- [x] Node в CI выровнен с Vercel (24)
- [x] Заголовки безопасности на хостинге (CSP пока в режиме Report-Only)
