# DESIGN.md — дизайн-система PinheadOS

> Обновляется в конце сессии при изменениях визуала (правило в CLAUDE.md).

## Язык: Order Studio (единый для обоих режимов)

Светлая «бумага» #FAFAFA, белые карточки, тонкие границы, синий акцент.
Тёмная тема — автоматически через токены `html[data-theme="dark"]`.

### Токены (src/index.css :root)
- Цвета: `--accent #2B2BF0` (электрик-синий) · `--signal #FF3B30` ·
  `--ink/--black #0A0A0A` · `--bg #FAFAFA` · `--card #fff` · `--surface #F5F5F5`
- Статусные пары: `--bg-success/--color-success`, `--bg-warning/…`, `--bg-error/…`
- Шрифты: **Barlow Condensed** (заголовки, 900 uppercase) ·
  **Inter** (текст; UI-подписи 10-11px, 600, letter-spacing 1.2-1.5px, uppercase) ·
  **Roboto Mono** (все числа: qty, KPI, счётчики, время)
- Логотип: Space Grotesk 700, letter-spacing 2px

### Шапка и навигация (оба режима)
- Белая шапка 52px, sticky, логотип слева
- Навигация — **вкладки на всю высоту** с бордером справа;
  hover/active = заливка `--accent` + белый текст
- ERP: второй ряд вкладок 44px; переключатели режимов: «✏️ ТЗ» / «🏭 Производство»

## Редизайн ERP (сессия 20, 6 фаз) — по макетам заказчика + UI Kit

Палитра ERP переопределена **на корне `.shell`** (ERP-scoped, Order Studio не трогаем; селекторы
`:global(html) .shell` / `:global(html[data-theme='dark']) .shell` выигрывают у :root/тёмной темы):
primary `#2563EB`, success `#10A34A`, warning `#F59E0B`, error `#EF4444`, +violet `#8B5CF6`/cyan `#06B6D4`,
светло-серый фон + белые карточки. Оболочка: вертикальный сгруппированный **Sidebar**
(`layout/Sidebar.jsx`, счётчики задач, сворачивание) + Header + прокручиваемый Main. Компонент
**Badge** (`components/Badge.jsx`). Чипы: в работе=синий (`chipProgress`), ожидает=амбер (`chipWaiting`).

Новые переиспользуемые примитивы (`erp/components/`): **Drawer** (правая боковая панель на токенах,
`useFocusTrap` + Escape, опциональные вкладки — монтируется родителем только когда открыта),
**Pagination**, **FilterBar** (поиск + чипы-вкладки + правый слот), **Stepper** (нумерованная
воронка со счётчиками), **Pipeline** (соединённые узлы-фазы со счётчиками). Детали заказа — единый
**OrderDrawer** (хук `useOrderDetail` общий со страницей `/orders/:id`); открытие из всех разделов
через `useOrderDrawer` + `orderLinkClick` (ЛКМ → Drawer, Ctrl/Cmd/Shift → диплинк). Dashboard —
KPI-плитки + «Заказы в работе»/«Загрузка цехов»/«Дедлайны»/«Быстрые действия»/«Уведомления».
Паттерны React Compiler: пагинация без `setState` в effect (`safePage = min(page, pageCount)`);
дешёвые производные с ранними return — инлайн, не в `useMemo`.

## Иконки и примитивы ERP (сессия 23)

**Иконки — свои, без зависимостей.** `erp/components/icons.js` хранит набор из 48 линейных иконок
24×24 **как данные** (`{t:'path'|'circle'|'rect', …}`), рендерит `Icon.jsx`. Правила набора: одна
толщина обводки (1.75), скруглённые концы, без заливки, `stroke="currentColor"` — цвет и размер
наследуются от родителя, поэтому активный пункт меню или кнопка перекрашивают иконку сами.
По умолчанию иконка декоративная (`aria-hidden`); одиночная и осмысленная — с `title` (→ `role="img"`).
**Эмодзи вместо иконок не использовать:** они по-разному рисуются в Windows/Android и ломают ритм.
Стрелка `→` внутри текста — типографика, остаётся. Иконки участков — `deptIcon(code)`
(`data/departments.ts`), значение — ИМЯ иконки, не глиф: ножницы/принтер/игла/утюг/щит.

**Метрики контролов.** Высоты примитивов заданы явно, тач-размер — своим
`@media (pointer: coarse)` в каждом CSS-модуле примитива: правило ≥44px в проекте ведётся
списком классов, и второй системы токенов высоты заводить не нужно.

| Примитив | Файл | Что даёт |
|---|---|---|
| `Button` | `components/Button.jsx` + `.module.css` | `variant` primary/secondary/ghost/danger, `size` sm/md/lg, `icon`, `iconOnly`, `loading`, `block`. Единый `:focus-visible`. **Заменяет глобальные `btn btn-*`** внутри ERP |
| `Field` | `components/Field.jsx` | label + control + hint/error, связка по id, `aria-invalid`/`aria-describedby` автоматом; `as` = input/select/textarea |
| `LoadFailed` / `EmptyResult` / `EmptyState` | `components/ErpStates.jsx` (+ `States.module.css`) | единые состояния раздела: сбой загрузки (`role="alert"` + «Повторить»), пустой результат подбора, пустой раздел |

У каждого примитива свой `*.module.css` — так монолит `erp.module.css` уменьшается по мере переноса,
без рискованной единовременной распилки.

## ERP-компоненты (erp.module.css)

| Паттерн | Классы | Правила |
|---|---|---|
| Статус-чипы | `chip` + `chipReady/Progress/Blocked/Done/Waiting/Neutral` | пилюли 999px, 11.5px/600 |
| Вкладки цехов | `deptTabs/deptTab/deptTabActive` + `deptTabCount` (красный бейдж `deptTabHot`) | скролл-бар без скроллбара |
| KPI-плитки | `kpiTile/kpiValue/kpiLabel` (+`kpiWarn/kpiDanger`) | значение — Mono 34px |
| Меню цехов (сайдбар) | группа «Цеха» в `Sidebar` + `navBadge` | все участки `QUEUE_DEPT_CODES` со счётчиком заданий (`readyCountFor`), пункт → `/queue/:deptCode`; логотип `.sidebarBrand` — ссылка на `/` (кликабелен весь блок, hover + focus-visible) |
| Канбан | `kanbanBoard/kanbanCol` (290px, flex-shrink:0 у лейнов!) `kanbanLane` (ready/in_progress/done) `kanbanCard` | 3 жеста: вертикально = приоритет, между дорожками = статус, **в чужую колонку = перенос в другой цех** (`kanbanColDroppable`, подтверждение с последствиями, комментарий при возврате/пропуске); дедлайн-точка `kanbanDot`; время-в-этапе; при drag страница не скроллится (`overscroll-behavior: contain` + автоскролл доски, `kanbanBoardDragging`) |
| Очередь цеха (строка) | `queueList/queueRow` + `queueRowMain` (grid из 8 колонок), `dragHandle`, `queueRowDropBefore/After` | три блока: В работе → Готово к запуску → Ожидает (блокировки внутри «Ожидает» со своей причиной), «Завершено недавно» свёрнуто; красным — только чипы срока/проблемы, не строка; перетаскивание меняет приоритет; развёрнутая строка = `StageActionsPanel`. **Grid не обрезает содержимое ячейки** — всё, что не влезло, рисуется поверх соседней колонки: `min-width: 0` обязателен КАЖДОЙ ячейке, `align-items: start`, а дата и просрочка идут двумя строками (`queueRowDue` — flex-column), иначе «31 июл · просрочен 31 дн.» наезжает на статусы |
| Очередь цеха (карточка) | `queueCard` + полоса слева (Ready зел./Progress жёлт./Blocked красн.), просрочка — красная рамка | мобильный вид (<760px): кнопки min-height 46px (планшет); блок «📋 ТЗ позиции» (📄 с именем и версией — если назначен PDF); превью 48px + лайтбокс; прогресс-бар qty_done N/M; фото брака/блока |
| План производства (/plan) | `planBoard/planDay/planDayHead/planDayStats` + `planCard` (+`planCard_*` по состоянию), `planCardWrap/planMoveBtns`, `planDeviations/planDeviationList`, `PlanSlotDrawer` (`planSection/planFormRow/planComment`), `PlanAddModal` (`planPickList/planPickRow`) | колонка = день недели, карточка = задача цеха; сводка дня стоит ДО карточек (план/факт/%/проблемы/просрочки/ждут материалы); вкладки «Все цеха» + участки; перенос между днями перетаскиванием И кнопками ←/→ (клавиатурная альтернатива обязательна); цвет карточки — только дополнение к текстовому статусу, «ожидает материалы» перебивает цвет просрочки, но сама просрочка показана отдельным чипом; блок «Требуют решения» внизу — система сама ничего не переносит |
| Ожидание материалов | `matWaitList/matWaitRow/matWaitName/matWaitFacts/matWaitNote` | под карточкой задания группы «Ожидают материалы»: что ждём, статус, ожидаемая дата, ответственный, дата обновления, комментарий + кнопка «Материал поступил» (право `material.receive`) |
| Маршрут и прогресс | `RouteProgress`: `routeProgress/routeTotal/routeList/routeRow/routeQty/routeBar` | «Раскрой 100/100 — завершено» построчно + общий процент; прогресс по штукам (`utils/progress`), не по числу этапов; текущая стадия — `routeRowCurrent` |
| Страница задания | `taskGrid` (2 колонки) + `taskFacts` (dl) | `/task/:stageId`: факты задания слева, маршрут справа, ниже ТЗ+действия, файлы, комментарии; номер заказа — ссылка; PDF-ТЗ цеха — карточкой `TzViewer` над структурным `TzBlock` |
| Фильтры заданий | `QueueFilters` (`FilterBar` + `filterPanel`) | 3 быстрых переключателя на виду (готово/просрочено/проблема), остальное в раскрытии; состояние в URL (`filtersToParams`), «Сбросить» появляется при активных |
| Админка | `AdminScreen`, 5 вкладок: Пользователи · Права · Цеха · Справочники · Заказы ТЗ | вкладка в `?tab=`; `PermissionsTab` — матрица право × роль чекбоксами; `DepartmentsScreen` — редактор участков (порядок, брендирование, руководитель, норматив); `DictionariesTab` — под-вкладки справочников + «Статусы» только для чтения |
| ТЗ в PDF | `TzViewer` (`tzDoc/tzDocHead/tzDocName/tzFrame/tzFullscreen*`) + `TzDocsSection` (`tzAssignRow/tzAssignDept/tzAssignMissing`) | просмотр — встроенный просмотрщик браузера (`iframe`), без npm-зависимостей: «Открыть ТЗ» (инлайн) · «На весь экран» (focus-trap + Escape) · «Скачать» · «В новой вкладке» (запасной путь для мобильного Safari); версия рядом с именем файла (`v2`), бейдж «ТЗ обновлено» — если версия появилась после старта этапа (в строке очереди тоже). **Одна загрузка на позицию**: `tzAssignRow` теперь справка «ТЗ увидят: Закрой · Швейка · ВТО · ОТК», а не список выпадающих выборов по цехам |
| Загрузка файла в форме | чипы состояния в `TzSection`: `chipProgress` «Загружается…» · `chipReady` «ТЗ загружено» · `chipBlocked` «не загрузилось: причина» + «Загрузить заново» | файл уходит в бакет при выборе, а не по кнопке отправки; сабмит заблокирован, пока есть незавершённые, со строкой-объяснением у кнопки. Показывать файл приложенным, пока его нет в Storage, нельзя |
| Назначение ТЗ по цехам | `tzAssignRow`: цех · `select` документа · версия | одна строка на производственный этап маршрута (закупка/склады ТЗ не требуют); «не назначено» — единственное красное; в форме создания те же строки, но до создания заказа (маршрут считается `buildItemRoute`) |
| Справочники в работе | `DictionaryDatalist` (datalist поверх свободного поля) + чипы `chipNeutral` | справочник — подсказка, а не ограничение: значение, которого нет, всё равно можно вписать; отключённые значения не предлагаются, но остаются в старых заказах |
| Карточка заказа | `matSection` секции; `stepper*` (точки этапов); `printBlock` (нанесение, синяя полоса слева) | InlineEdit: клик→input→Enter |
| Формы | `tile/tileActive` (плитки-radio) · `accSection/accHeader` (аккордеон) · `inputError/fieldError` (инлайн-ошибки) · `draftBanner` (черновик) · `dropZone` (превью, Ctrl+V) · `sizeGrid` + чипсы-пресеты | noValidate + инлайн-валидация с автоскроллом; автосейв черновика; focus-trap + Escape |
| Таблицы | `tableWrap/table` + `Pagination`/`FilterBar` (`erp/components/`) | th: Inter 10px uppercase, фон surface; пагинация «Показывать по N»; FilterBar = поиск + чипы-вкладки со счётчиками + правый слот |
| Боковая панель | `Drawer` (`erp/components/`): `drawerOverlay/drawerPanel/drawerHead/drawerTabs/drawerTab/drawerBody` | правый оверлей на токенах, `useFocusTrap`+Escape, вкладки; монтируется только когда открыта |
| Карточка заказа (Drawer) | `OrderDrawer`+`OrderDrawerHost` (7 вкладок: Информация/Маршрут/Материалы/ТЗ/Файлы/Комментарии/История) | хук `useOrderDetail` общий со страницей (страница keyed `key={orderId}` — сброс при A→B); ТЗ = `TzBlock defaultOpen hideToggle`; Файлы = `order.attachments`→`getPublicUrl('erp-attachments')` (`.fileGrid`/`.fileCard`); `useOrderDrawer`+`orderLinkClick` (ЛКМ→Drawer, Ctrl/Cmd→диплинк) |
| Глобальный поиск + колокол (шапка) | `useErpSearch` (стор) + `ErpLayout` | поле шапки и поиск «Заказов» — один источник, Enter→`/orders`; 🔔 → `/orders?filter=overdue`; «Новый заказ» дашборда → `/orders?new=1`→модалка |
| Приёмка склада (4.1.3) | блок «Поле / План / Факт» на материал (в `matSection`) | план read-only из закупки; факт — inputs (`fact_*`/кол-во) + статус приёмки + комментарий |
| Индикатор стадий | **`StageIndicator`** — один компонент, три вида: `dots` (`stepper*`), `funnel` (`numStepper*`), `pipeline` (`pipe*`) | Виды отвечают на разные вопросы и сохранены осознанно: `dots` — «где сейчас эта позиция» (точки маршрута, ✓ у пройденных), `funnel` — «сколько операций в фазе» (нумерованная воронка со счётчиками), `pipeline` — «как разработки распределены» (узлы со стрелками + боковой узел). Реализация одна: раньше три независимых компонента требовали трёх правок на одну |
| Подряд (4.2.4) | `StageIndicator variant="funnel"` + `table` | текущий статус и следующее действие — в РАЗНЫХ колонках; сверху нумерованный степпер-воронка готового изделия со счётчиками по 6 статусам |
| Эксперимент | `StageIndicator variant="pipeline"` | Лекала→Проработка→Примерка→Готов к серии; активные (count>0) — акцентные; «Возврат конструктору» — пунктирный красный, вне цепочки (без стрелки) |
| Сортировка таблиц | `SortableTh` + `useTableSort`/`sortRows` (`utils/tableSort`) | заголовок — кнопка с `aria-sort` и стрелкой (↕ у неактивной); клик: возрастание → убывание → без сортировки; **пустые ячейки всегда внизу** при любом направлении, порядок стабильный; сортируем по тому, что видно в ячейке (статус — подписью), и ДО пагинации |
| Поле даты | `DateField` (`dateHint`/`dateEcho`/`datePresets`) | нативный `type="date"` (на планшете системный календарь) + эхо «14 авг. 2026» под полем, у пустого — «дд.мм.гггг», на сроках кнопки «Сегодня / +3 дня / +7 дней» |

## UX-правила
- Действие цеха = 1 тап; тач-таргеты ≥44px (через токены `--control-h*` в `@media (pointer: coarse)`)
- Длинная форма внутри карточки списка — запрещена: выносить в `Drawer` пошаговым мастером
  (образец — `queue/DefectWizard.jsx`: «сколько и почему» → «куда вернуть»)
- Пустой экран — `EmptyState` с подсказкой что делать, а не серый текст; ошибка загрузки —
  `ErrorState` с «Повторить» на каждом экране, где есть `loadError`
- Элементы длинных списков — под `React.memo`, а обработчики к ним — под `useCallback`
  (без второго первое бесполезно)
- Просрочка — красный `--color-error`, ≤3 дней — жёлтый `--color-warning`
- Красным выделяется **только** просрочка или проблема (чип/колонка), а не вся строка/карточка
- Показатели (KPI) — не только статистика: плитка кликабельна целиком и фильтрует список
  (одна позиция → сразу её карточка), активный фильтр виден и сбрасывается
- Возврат из карточки заказа восстанавливает контекст: цех (маршрут), фильтры (URL),
  позицию прокрутки (`useScrollRestore`); ссылки на заказ именуются номером, не «Открыть на странице»
- Оверлей, который человек может захотеть переслать или обновить (боковая карточка
  заказа), живёт в адресе: `?order=<id>`. Открытие пушит запись истории, закрытие
  снимает её же — «Назад» и ✕ делают одно и то же. Пришли по ссылке сразу
  с параметром — своей записи нет, закрытие идёт через `replace`, иначе ✕ уносит
  человека на прошлый сайт
- Необратимые переносы (пропуск этапа, возврат назад, незакрытое количество, возврат
  брака через промежуточные этапы, приёмка «полностью» при недостаче) — подтверждение
  с перечислением последствий **по именам**: какие этапы откатятся, сколько штук
  останется, чем факт расходится с планом. Возврат и пропуск требуют комментария в историю
- Перетаскивание не должно возить страницу: скролл держат доска и колонки
- Горизонтальный скролл всегда с подсказкой края: `ScrollHintBox` (или `useScrollHints`
  для готового контейнера) рисует градиент с той стороны, где есть скрытое содержимое —
  иначе колонки справа выглядят отсутствующими, а не спрятанными
- Кнопка цехового действия блокируется на время запроса (`busy`): планшет отдаёт
  двойные тапы, а действия этапа не идемпотентны
- Длинная история/лента показывается частично, с кнопкой «Показать все (N)»
- `title` ставится только на текст, который реально обрезается, и не повторяет
  то, что уже видно рядом (номер, статус) — иначе подсказка бесполезна
- Значения справочников не удаляются, а отключаются — уже записанные в заказах
  остаются читаемыми; редактируемый справочник подсказывает, но не запрещает свой ввод
- Ошибка формы: инлайн у поля (красная рамка + текст, aria-invalid) + автоскролл
  к первому ошибочному; у кнопки — «Осталось заполнить: …» (не заполнено) и
  «Проверьте: …» (заполнено неверно). Проверка живёт в валидаторе, не в сабмите:
  тост исчезает, а рамка и автоскролл — нет
- `role="tab"` только в полном комплекте: `aria-controls` + `role="tabpanel"` +
  roving tabindex + ←/→. Иначе — обычные кнопки с `aria-pressed`
- У каждого перетаскивания есть клавиатурная альтернатива (кнопки ↑/↓, Enter/Space)
- Инлайн-правки вместо модалок там, где правится 1 поле (карандаш при hover)
- Optimistic UI с rollback + защита от race с realtime (pendingMutations)
- toast на каждую ошибку Supabase
- Короткие имена цехов (deptShortName) во всех чипах: Закрой, Швейка, ДТФ…
- Mobile: брейкпоинты 760/480; <760 модалки полноэкранные (sticky-кнопки),
  список заказов — карточки; канбан — touch-DnD (mobile-drag-drop) + scroll-snap.
  Кнопки модалки залипают и на десктопе: длинная форма создания заказа уводила
  «Создать» за пределы экрана
- Загрузка: скелетоны (`ErpSkeletons.jsx`), не текст «Загрузка…». Форма скелетона
  повторяет финальный лейаут буквально: дашборд — 6 `.kpiCard` + два ряда виджетов,
  очередь — строки на десктопе и карточки на мобильном, канбан — колонки
- Три состояния экрана рисуются в этом порядке: **ошибка** (`LoadFailed` с кнопкой
  «Повторить») → **скелетон** → **пусто**. Скелетон вешать на `!loaded && !loadError`,
  а не на `loading`: при сбое `loading` уже false, и экран замирал навсегда.
  Пустое состояние обязано различать «работы нет» и «под фильтры ничего не попало»
  (`EmptyResult` с запросом и кнопкой «Сбросить»)
- Доступ к действию проверяется **по действию**, а не по цеху: `useStagePermissions`
  даёт `take/progress/complete/block/defect`, каждая кнопка гейтится своим правом.
  Всякое право матрицы обязано что-то выключать — иначе админка врёт
- Названия: line-clamp 2 + title с полным текстом
- Мелкий текст ≥12px; оверлей/тени модалок — токены `--overlay`/`--shadow-modal`
- Контраст: текст статуса берётся из `--color-*-ink`, заливка — из `--bg-*`.
  Один и тот же токен на текст и на фон не проходит AA (проверено расчётом)
- Тач-таргеты ≥44px ведутся списком в `@media (pointer: coarse)` (`erp.module.css`);
  новый интерактивный класс дописывается туда же. Инлайновые размеры запрещены —
  они перебивают медиазапрос
- PWA: manifest + иконки 192/512, установка на планшеты цехов

## Импортированные паттерны kontora24
Время-в-статусе (мин/ч/дн) · дедлайн-точки · блокировка DnD-зон затемнением ·
уникальные имена realtime-каналов (`crypto.randomUUID()`) + debounce 500ms ·
error-summary + плитки · Ctrl+V paste превью · InlineEdit · единая история
(статусы+audit) · чат заказа.
