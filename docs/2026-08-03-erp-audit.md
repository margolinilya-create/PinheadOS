# Аудит Pinhead ERP — 03.08.2026

Карта проекта, замеры и план работ. Ветка `claude/pinhead-erp-audit-optimize-7ol921`.
База: Supabase `pinhead-os-v2` (`glhwbktsokphgksdvcxj`).

## Baseline

| Проверка | Результат |
|---|---|
| `npm run lint` | 0 ошибок |
| `npm run test` | 1639 тестов / 83 файла — зелёные |
| `npm run build` | успешно |
| Исходников | 342 файла, 34 659 строк (без тестов) |

### Бандл (сборка HEAD, gzip)

| | JS | CSS |
|---|---|---|
| Вход (index.html + modulepreload) | 183 814 Б | 23 364 Б |
| **Оболочка ERP** (вход + ErpApp + примитивы) | **244 035 Б** | **35 816 Б** |

Критерий приёмки «initial JS < 250 кБ gzip» **выполнен на момент аудита** — 244 кБ.
Цифры из постановки задачи (280/168/168/158 кБ, ~800 суммарно) — это **несжатые**
размеры тех же чанков; они сошлись до килобайта.

Не в критическом пути: `vendor-charts` (64 кБ gzip, только аналитика Order Studio),
`OrderStudioApp` (57 кБ, за флагом), 10 ленивых экранов ERP.

Прежний страж бюджета (инлайн в `ci.yml`) считал только файлы из `dist/index.html`
и не видел `ErpApp` + общий чанк примитивов + их CSS: рапортовал 207 кБ при
реальных 280. Заменён на `scripts/bundle-budget.mjs`, считающий по манифесту
сборки два графа — вход и оболочку.

### Сеть

Загрузка `/` → открытие карточки заказа = **11 REST + 2 WebSocket**:

| Что | Откуда |
|---|---|
| departments, orders | `loadAll` (ErpLayout) |
| role_permissions, dictionaries, subcontracting, experimental | ErpLayout, эффект «бейджи стабильны на любом экране» |
| employees (мой цех) | `loadMyDept` |
| loadOne, stage_events, order_audit, order_comments | `useOrderDetail` |
| канал realtime, канал комментариев | `subscribeRealtime`, `useOrderDetail` |

`ORDER_LIST_SELECT` — один запрос с 10 вложенными отношениями по ВСЕМ активным
заказам: 76 заказов / 197 позиций / 443 этапа ≈ 350 кБ JSON.

## Карта

### Роуты ERP (`src/erp/ErpApp.jsx`)

Статикой: `ErpLayout`, `ErpDashboard`, `OrdersScreen`, `DepartmentQueue`.
Ленивые: `OrderCard`, `ProductionBoard`, `AdminScreen`, `ProductionTask`,
`FabricPurchasing`, `Warehouse`, `Subcontracting`, `Experimental`, `DeptLoad`,
`PlanScreen`.

### Слой данных — корень большинства находок

Единственный источник — `useErpStore` (composition root + 14 слайсов). Все экраны
читают ОДИН массив `orders` и фильтруют его в памяти. Следствия:

* серверной пагинации нет нигде, кроме архива заказов и двух экранов склада/закупки;
* сортировка и фильтрация — клиентские, на полном наборе;
* кэша, дедупликации и отмены запросов нет — их роль играет realtime;
* любой экран платит за данные всех остальных экранов.

## Находки

### Ф2 — данные

**F1. Дубли заказов — реальные строки в БД, не баг рендера.**
5 групп по `bitrix_id`: `22222` (×4), `212112`, `213`, `213231`, `213312` (×2).
Разные UUID, одинаковые названия, созданы с интервалом 25–80 секунд.
Ключи React — `o.id`, стабильные; картезиан от PostgREST-эмбедов невозможен;
guard `saving` в форме есть, двойной клик исключён. Причина: **ничто не мешает
создать второй заказ с тем же № сделки.**

**F2. Тестовые данные в проде.** 36 заказов из 147 (26 из 76 активных).

**F3. Просрочка.** 47 из 76 активных. Разбивка: 1–7 дней — 6, 8–30 — 39, 30+ — 2,
без срока — 19. Градации в интерфейсе нет, метрика не отвечает на вопрос
«что делать сейчас».

**F4. Пустые `/plan` и `/load` — потому что данных нет.** `erp_calendar_slots` — 2
строки, `erp_tz_documents` — 0, вложений — 1. `planPercent` при `qty_planned = 0`
возвращает **100** (`utils/planCard.ts`, то же в `utils/planDay.ts`) — отсюда
«100 %» на пустой неделе.

### Ф3 — дизайн-система

**F5. Примитивы заявлены, но не применены.** `erp/components/Button` импортируют
**2 файла**; `className="btn …"` (язык Order Studio) — **107 мест внутри ERP**.
Инлайновых `style={{}}` в ERP — **95**.

**F6. Контраст (посчитано по WCAG 2.1).**

| Токен | Фон | Контраст | AA |
|---|---|---|---|
| `--text-muted` #888888 | белый | 3.54:1 | ✗ |
| `--text-muted` #888888 | `--bg` #FAFAFA | 3.40:1 | ✗ |
| `--text-muted` #777777 (тёмная) | `--card` #1E1E1E | 3.72:1 | ✗ |
| `--text-dim` #888888 (тёмная) | `--surface` #252525 | 4.32:1 | ✗ |
| `--text-dim` #888888 (тёмная) | `--card` #1E1E1E | 4.71:1 | ✓ |
| `--text-dim` #666666 (светлая) | белый | 5.74:1 | ✓ |

Отсюда «местами уходит ниже» — один и тот же токен проходит на карточке и
не проходит на поверхности. Чинится в токенах, не по местам.

### Ф5 — адаптив

**F7. Брейкпоинты:** 768 (16 правил), 480 (11), 760 (4), 1100 (2), 640 (1),
600 (1), 1024 (1).

### Ф6 — качество

**F8. TypeScript:** `strict: false`, `checkJs: false`. Типы схемы Supabase
написаны руками (`erp/types.ts`), из БД не генерируются; расхождение ловит
только `orderSelect.test.ts`.

## RLS: расхождения клиента и сервера

Стражи-триггеры **есть** и делят UPDATE по колонкам: `erp_stage_guard` (этапы),
`erp_material_guard` (материалы), `erp_calendar_guard` (план). Ниже — то, что
осталось непокрытым.

| № | Риск | Факт |
|---|---|---|
| **R1** | сервер мягче клиента | `erp_orders` UPDATE = `erp_is_member()`, стража нет. Любой сотрудник ERP может через REST изменить `title`, `customer`, `due_date`, `manager`, `status`, `shipped_*` любого заказа. Клиент гейтит это правом `order.manage` |
| **R2** | сервер мягче клиента | `erp_order_items` UPDATE = `erp_is_member()`, стража нет: `qty`, `product_type`, `notes` правит кто угодно |
| **R3** | сервер строже клиента | `erp_orders` DELETE = `is_admin()`, а кнопку «Удалить» клиент показывает при `order.manage` (`OrdersScreen.jsx:101`). Менеджер с правом, но без роли admin получит 42501 — «кнопка есть, сервер отказывает» |
| R4 | мусор | `erp_tz_assignments` (устарела, пуста) держит 4 политики и 2 неиспользуемых индекса |
| R5 | advisor | `erp_has_permission(perm text)` вызываема ролью **anon**. Правило проекта («функции без аргументов, возвращают булево о самом вызывающем») её не покрывает — у неё есть аргумент |
| R6 | advisor | Auth: защита от утёкших паролей (HaveIBeenPwned) выключена |
| R7 | advisor perf | 19 неиспользуемых индексов (уровень INFO) |

Утечки чтения нет: SELECT везде `erp_is_member()`, записи снабжения —
`erp_is_manager()`, административные — `is_admin()`.

Секретов в клиентском бандле, кроме публичного anon key, нет: только
`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` и опциональный
`VITE_ERROR_REPORT_URL`.

**Ничего в политиках не менялось** — по договорённости это отдельное решение.

## Решения заказчика (03.08.2026)

1. **Порядок фаз:** Ф0 → Ф2 → Ф3 → Ф1′ → Ф4 → Ф5 → Ф6. Сначала то, что видно
   людям каждый день; слой данных позже — при 76 заказах он ещё не жмёт,
   а бюджет бандла уже выполнен.
2. **Навигация:** один раздел «Производство» с вкладками Доска · План · Загрузка.
   `/plan` и `/load` редиректят на вкладки, `/board` переименовывается в
   «Доска производства» — конфликт заголовков исчезает.
3. **Просрочка:** срок прошёл **И** заказ не готов к отгрузке. Готовый, но не
   отгруженный заказ ждёт логистики, а не производства. Плюс градация
   1–7 / 8–30 / 30+ дней в интерфейсе.
4. **Тестовые данные:** флаг `is_demo` + фильтр в UI, по умолчанию скрыты,
   переключатель для admin/director. Ничего не удаляется.

## Что уже было сделано до аудита

Чтобы не переделывать: ленивые чанки ERP (10 экранов), скелетоны вместо
полноэкранного спиннера, `ErrorBoundary` на роут, `ScrollHintBox`, `Pagination`
(склад, закупка), примитивы `Badge`/`Drawer`/`Button`/`Field`/`Stepper`,
единые состояния `LoadFailed`/`EmptyResult`/`EmptyState`, skip-link, focus-trap
в модалках, `aria-live` у тостов, фильтры заказов в URL, `useScrollRestore`,
ленивый архив постранично, точечный realtime вместо полной перезагрузки.

---

## Результат: что закрыто (вторая волна)

| Метрика | До аудита | После |
|---|---|---|
| Запросов: загрузка + карточка заказа | 11 REST + 2 WS | **4 REST + 2 WS** |
| Запросов на открытие карточки | 4 | **2** |
| Юнит-тестов | 1639 | **1754** |
| e2e (desktop) | 69 | **96** |
| Ошибок `tsc --noEmit` | 84 (`strict: false`) | **0** (`strict: true`) |
| Сырых кнопок `btn btn-*` в ERP | 109 | **0** |
| Токенов ниже WCAG AA | 3 | **0** |
| Брейкпоинтов в CSS | 7 | **3** |
| Оболочка ERP, JS gzip | 244.0 кБ | **240.1 кБ** (бюджет 250) |
| Тестовых заказов в рабочем списке | 37 из 76 активных | **0** (39 боевых активных) |

### RLS: закрытые риски

| № | Было | Стало |
|---|---|---|
| R1 | `erp_orders` UPDATE = `erp_is_member()` без стража | Триггер `erp_order_guard`: поля — `order.manage`, `is_demo` — `is_admin()`, отгрузка не гейтится (как в интерфейсе) |
| R2 | `erp_order_items` UPDATE открыт всем | Триггер `erp_order_item_guard`: `order.manage` |
| R3 | Кнопка «Удалить» при `order.manage`, политика требует `is_admin()` | Клиент сведён к admin/director |
| R5 | `erp_has_permission(text)` вызываема `anon` | Отозвано у PUBLIC, явный грант `authenticated` |
| R4 | `erp_tz_assignments` держит 4 политики | **Не трогали**: таблицу использует `erp_create_order`, удаление сломало бы создание заказа |
| R6 | Leaked-password protection выключена | **Не закрыто**: настройка Auth, меняется в консоли Supabase, не миграцией |

Клиент правился ВМЕСТЕ со стражем. Оказалось, что инлайн-правки заказа
не гейтились и на клиенте тоже — то есть страж без этой правки создал бы
запрещённый правилами отказ «кнопка есть, действие падает».

### Разметка тестовых данных

Признак взят **из данных**, а не из названия: 16.07.2026 импортированы 100
боевых заказов, все с реальными менеджерами; всё, что заведено позже, —
47 заказов, и ни у одного реального менеджера нет. Поэтому «Тест
вышивки_10.07» и «для Стаса_пошив» (16.07, менеджер Иванова Мария) остались
боевыми: эвристика по слову «тест» спрятала бы их.

### Что нашли сами проверки

Не «написали тест и он позеленел», а поймали настоящее:

* **e2e** упал дважды после смены навигации и вкладок — оба спека обновлены
  под новый интерфейс, а не наоборот.
* **Мок Supabase** не знал про пакетные RPC и отдавал пустой массив:
  приложение держалось на запасном пути в `loadAll`, то есть e2e проверял
  не тот путь, по которому ходит прод.
* **Прогон доступности** нашёл, что `aria-live` у тостов стоял, а объявления
  не было: контейнер монтировался вместе с первым сообщением, а такой регион
  скринридер не отслеживает.
* **strict** показал, что `stageDone` объявлял `qty_done: number`, хотя код
  читает его через `?? 0`, а колонка nullable; что `DefectStage` не объявлял
  полей, которые слайс уже читал; что фикстура `selectSku.test` не была `SkuItem`.
* **Страж ключей хранилища** поймал незадекларированный `erp_show_demo`.
* **Страж отзывов EXECUTE** требовал дописывать отзыв в уже применённую
  миграцию — то есть править историю; расширен на все миграции.

## Что осталось несделанным

* **Серверная пагинация активного списка.** Не сделана осознанно: активный
  набор — рабочая память приложения, его целиком читают канбан, очередь,
  дашборд и план. Пагинация активных сломала бы их, а фильтр «Готов
  к отгрузке» вычисляется из этапов и материалов и на сервере потребовал бы
  ВТОРОЙ реализации `isOrderReadyToShip` — ровно того расхождения, которое
  проект запрещает. Архив (растёт без ограничений) страницами грузится давно;
  список получил пагинацию, сортировку и контекст в адресе.
* **Массовые действия и сохранённые представления** в списке заказов.
* **Общий `DataTable`.** Таблицы уже на одном классе и одних примитивах
  (`SortableTh`, `Pagination`, `ScrollHintBox`, `EmptyResult`); обёртка
  поверх них — рефакторинг шести экранов с реальным риском ради умеренной
  выгоды.
* **Замеры LCP/CLS.** Нужен прогон против задеплоенной сборки с реальной
  авторизацией; из окружения разработки цифры были бы синтетическими.
* **axe/Lighthouse.** Пакета в дереве нет, зависимости без обсуждения
  не добавляются. Прогон доступности сделан нативно по перечисленным
  в задаче критериям.
* **e2e сценариев записи** (создание заказа, движение по этапам, приёмка,
  отгрузка): мок Supabase read-only и не отражает изменения обратно. Логика
  покрыта юнитами стора; чтобы покрыть e2e, нужен пишущий стенд.
