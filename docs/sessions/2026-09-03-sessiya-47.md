## Состояние на 2026-09-03 (сессия 47) — сплошной аудит ERP закрыт

Раздел «Производство» пройден целиком: UX, доступность (WCAG 2.1 AA) и логика.
Полный разбор с местами в коде — `docs/2026-09-03-erp-audit.md`; здесь только
то, что нужно помнить дальше.

**Что было опаснее всего.** Форма отчёта цеха закрывала этап мимо гейта
завершения, причём ровно у тех участков, у которых стоит материальный гейт:
закрой закрывался при неприехавшей ткани. Правило теперь живёт у ПИСАТЕЛЕЙ
(`stagesSlice.completionBlockFor` — `setStageStatus`, `reportProgress`,
`submitStageReport`) и на сервере (`erp_stage_completion_block` +
пересозданная `erp_stage_submit_report`, миграция `20260903203310`).
Пятый путь закрытия пройдёт через тех же писателей, вписывать его никуда
не нужно.

**Открытая работа, названная вслух.** Серверного гейта отгрузки по-прежнему
нет: `isOrderReadyToShip` живёт только на клиенте. Зеркало правила состоит
из четырёх частей (все этапы закрыты, материалы разобраны, разработки закрыты,
аварийное снятие `ship_gate`), и неверное зеркало заморозило бы отгрузку
целиком — работа требует своей проверки на живой базе от лица кладовщика.

**Палитра `.shell` изменилась** (решение владельца — AA везде, включая
границы): `--text-dim`/`--text-muted` → `#666E7B`, `--border-control` `#868E9B`
(тёмная тема `#66748C`), `--btn-danger-bg` `#DC2626`. Перегенерированы три
визуальных эталона из десяти; расхождения объяснены в отчёте.

**Автопроверка доступности** — `e2e/erp-axe.spec.ts`, 12 экранов, уровни A
и AA, гоняется на desktop и mobile. Список осознанных исключений `KNOWN` пуст,
и это его нормальное состояние.

**Журнал миграций** снова отставал от прода (третий раз: 13.08, 18.08, 02.09).
Файл `20260902002826_erp_order_done_without_due.sql` восстановлен подлинным
текстом из `supabase_migrations.schema_migrations.statements`; снимок
`APPLIED.json` — 200 записей, md5 `e02802560f972115afc60bd9aac8fba9`,
совпадает с продом точно.

---
