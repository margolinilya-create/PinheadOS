## Состояние на 2026-09-04 (сессия 48) — прогон ERP, три находки закрыты

Сплошной прогон существующими средствами: lint, typecheck, 3550 unit, сборка,
бюджет, 357 e2e — **всё зелёное, функциональных регрессий нет**. Все три
находки пришли НЕ из тестового набора, и это главный вывод прогона.

**1. Обход гейта прав.** `erp_ensure_subcontract_send` — `security definer`
без внутренней проверки — была вызываема любым вошедшим через
`/rest/v1/rpc/` и заводила складскую задачу мимо гейта
`warehouse.manage`/`order.manage`. Проверено на живой базе по шаблону
`docs/erp/permission-check.sql`: рабочий получал 42501 на прямой вставке
и успешно создавал ту же задачу через функцию.

Грант `authenticated` был выписан 24.08 по шаблону правила про функции
**в предикатах RLS** — но эта в предикатах не стоит. Починка — отзыв
(`20260904061100`), а НЕ гейт внутри: внутренняя проверка закрыла бы и цеховой
путь, ради которого функция и сделана `definer`. Оба вызывающих триггера сами
`security definer`, поэтому отзыв их не касается — сверено с `pg_proc.prosecdef`.
Сторож — `subcontractSend.test.ts`, проверен мутацией.

**2. Воскресшая RLS-политика.** `20260824185639` создала `erp_att_delete_dev`
заново — ту, которую `20260821100000` намеренно удаляла ради правила «одна
политика на команду». Предикаты успели разойтись: `dev_task` был только
в воскресшей. Сведено обратно (`20260904061108`), сторож — `attachmentKinds.test.ts`,
проверен двумя мутациями.

**3. `npm run audit` кричал впустую.** Пять находок, все ложные: hex-коды
внутри CSS-комментариев (скрипт не снимал комментарии) и «непокрытая»
`fadeSlideIn` — универсальный блок `*` он обрабатывает, но проход был ОДИН,
и `utils.css` читался раньше `wizard.css`, то есть покрытие зависело
от алфавитного порядка файлов. Скрипт падал с кодом 1 и в CI не стоял.
Починен (два прохода + `stripCssComments` с сохранением нумерации строк),
проверен мутациями и **добавлен в CI**.

`migrations:verify` в CI намеренно НЕ добавлен: живую базу из CI не прочитать.
Сверка снимка с продом сделана руками — 202 записи, md5 совпал.
