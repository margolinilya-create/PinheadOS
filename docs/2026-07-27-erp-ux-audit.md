# UI/UX-аудит ERP — находки и план устранения

**Дата:** 2026-07-27 · **Область:** только 🏭 Производство (`src/erp/**`), Order Studio не трогали.

## Как проверяли

1. **Скилл `frontend-design`** + `docs/DESIGN.md` («UX-правила» и таблица ERP-компонентов)
   как измерительная линейка — сверяли не «нравится/не нравится», а соответствие
   собственным правилам проекта.
2. **Живой прогон**: 13 скриншотов через Playwright с мок-Supabase — десктоп 1440,
   мобильный 390, тёмная тема. Временная спека удалена.
3. **5 агентов** по зонам: очередь цеха и задание · доска/канбан/дашборд/навигация ·
   заказы и форма создания · админка и сопровождающие циклы · дизайн-система.
   Каждому запрещено докладывать находку без ссылки «файл:строка».
4. **Проверка руками**: каждый P0 и ключевые P1 перечитаны в коде лично; несколько
   заявок агентов отсеяны как неточные и в этот отчёт не попали.

**Итог:** 9 × P0, 38 × P1, ~50 × P2.

Контраст считали по формуле WCAG 2.x (sRGB → linear luminance), не на глаз;
все приведённые ниже цифры пересчитаны лично.

---

## P0 — ломает работу или портит данные

### 1. Перенос между цехами на канбане молча не срабатывает на ~90% площади колонки
`erp/components/ErpKanban.jsx:118-124` + `:234-238`

Дорожка ловит `drop` первой и синхронно зовёт `onLaneDrop` → `onDragEnd()` → `dragRef.current = null`.
Затем событие всплывает в `<section>` колонки, `onColumnDrop` читает уже `null` и тихо выходит (`:148-150`).
`stopPropagation` при этом не вызывается: для чужого цеха `droppable === false` и `dropAt === null`.

Перенос работает **только** если отпустить карточку ровно на шапке колонки (~40px) —
остальное занято тремя дорожками. Колонка при этом подсвечена как валидная зона приёма:
пользователь видит приглашение, бросает — и не происходит ничего, даже тоста.

**Фикс:** в `onDrop` дорожки выходить сразу, если `dragRef.current.stage.department_id !== deptId`
(отдать событие колонке). Плюс компонентный тест на порядок drop-событий — сейчас
`kanbanColumns.test.js` покрывает только чистую группировку.

### 2. Клик по чипу этапа на производственном плане закрывает этап без прав и без подтверждения
`erp/screens/ProductionBoard.jsx:53-69` и `:149-152`

```jsx
const onAdvance = async (stage, nextStatus, item) => {
  const extra = nextStatus === 'done' ? { qty_done: item.qty } : {};
  await setStageStatus(stage.id, nextStatus, extra);
};
```

`ProductionBoard.jsx` — единственный экран действий, который **не импортирует `useErpAccess`**.
Один промах мышью по 23-пиксельному чипу в плотной таблице: этап закрыт, `qty_done` затёрт
полным тиражом (сдали 300 из 500 → станет 500), зависимые этапы разблокированы.
Нарушены оба правила: «доступ только через `useErpAccess`» (CLAUDE.md) и «молча этап не закрывать».

**Фикс:** гейт `access.canDo(...)` на чипе + `confirm()` с последствиями; количество —
через `reportProgress`, а не подстановкой `qty_done: item.qty`.

### 3. «✓ Завершить этап» в очереди цеха записывает весь тираж без подтверждения
`erp/screens/queue/useStageActions.js:37-40`

```js
const onDone = useCallback(
  (entry) => setStageStatus(entry.stage.id, 'done', { qty_done: entry.item.qty }), …);
```

Один тап, без диалога. Записал 40 из 100, нажал «Завершить» — в БД уйдёт 100.
`useConfirmStore` в зоне очереди не используется ни разу.
DESIGN.md прямо требует подтверждения именно для «незакрытого количества».

**Фикс:** при `qty_done < item.qty` — `confirm()`: «Останется незакрытыми N шт, они запишутся
как выполненные; следующий этап (X) откроется на весь тираж».

### 4. Фильтры производственного плана в табличном виде ничего не фильтруют
`erp/screens/ProductionBoard.jsx:133-147`

`QueueFilters` рендерится для обоих видов, но `rows` использует из `filters` **только `filters.q`**.
«🟢 Готово к запуску», «⏰ Просрочено», «🚫 С проблемой», цех, статус, исполнитель, срок с-по,
сортировка — мертвы. При этом чип красится синим и появляется «Сбросить».

Диспетчер жмёт «Просрочено», видит те же 200 строк и делает вывод, что просрочено всё.
Фильтр, который врёт, хуже отсутствующего.

**Фикс:** строить строки из `applyStageFilters(buildQueueEntries(...))` либо (минимум)
скрывать неработающие контролы в табличном виде.

### 5. Матрица прав врёт про админа — снятая галочка в колонке «Директор» отключает доступ ему самому
`erp/screens/admin/PermissionsTab.jsx:43-47` vs `erp/store/useErpAccess.ts:53-56`

Подпись обещает: «Администратор и директор имеют полный доступ независимо от галочек».
Код: `resolveErpRole('admin')` → `'director'`, а `can = isDev || isAllowed(matrix, role, permission)` —
никакого обхода для руководства нет. Админ снимает галочку, доверяя подписи, и молча
теряет право сам, одновременно у себя и у директора. Отката в UI нет.

**Фикс:** либо реально `can = isPrivileged || isAllowed(...)`, либо убрать ложную подпись
и показать колонку «Директор» read-only с замком.

### 6. 7 из 10 прав в матрице ничего не выключают
`erp/types.ts:685-688` объявляет 10 прав, проверяются три:
`stage.move_department` и `stage.priority` (`ErpKanban.jsx:92,110`), `tz.manage` (`TzDocsSection.jsx:65`).

`stage.take`, `stage.progress`, `stage.complete`, `stage.block`, `stage.defect`, `order.manage`,
`catalog.edit` — нигде. В очереди гейт один: `canAct = access.canActIn(dept?.id)`
(`DepartmentQueue.jsx:145`). Хуже: `canActInDept` возвращает `true`, если привязки к цеху нет
(`utils/permissions.ts:87`) — значит менеджер без записи в `erp_employees` жмёт «Взять в работу»,
«Завершить», «Брак» в **любом** цехе.

Руководитель снимает «Оформлять брак», видит галочку снятой, считает вопрос закрытым — а кнопка
у рабочего осталась. Матрица создаёт ложное чувство контроля.

**Фикс:** провести `can('stage.take'|'stage.complete'|…)` в `StageActionsPanel`/`useStageActions`
и `catalog.edit` в админские вкладки — либо временно показывать только 3 работающих права.

### 7. Повторная попытка создать заказ после сбоя падает всегда — форма превращается в тупик
`erp/screens/orders/CreateOrderModal.jsx:304-315`

Путь ТЗ детерминированный (`tz/new/<groupId>/v1-<имя>`), `groupId` генерируется один раз
при добавлении файла (`:113`), `upsert: false`. Если RPC `erp_create_order` упал или сорвалась
загрузка 2-го файла из 3 — повторное нажатие грузит **все** файлы заново, первый уже лежит
по тому же пути → Duplicate → «Не удалось загрузить ТЗ … — заказ не создан». Сколько ни жми,
будет то же самое. Спасает только неочевидное «убрать и приложить PDF заново».

**Фикс:** новый `groupId` (или суффикс попытки) перед каждой загрузкой либо `upsert: true`;
в тексте ошибки назвать действие.

### 8. Ошибка загрузки = вечный скелетон или пустой экран на 6 экранах
Флаг `loadError` в сторе есть (`ordersSlice.ts:59`) и корректно обработан на трёх экранах
(`Warehouse.jsx:167-172`, `Subcontracting.jsx:230`, `FabricPurchasing.jsx:315`). Игнорируют его:

| Экран | Что видит пользователь |
|---|---|
| `DepartmentQueue.jsx:312` | «Выберите свой цех выше» навсегда |
| `ProductionTask.jsx:79` | скелетон крутится вечно |
| `ErpDashboard.jsx:160` | скелетон крутится вечно |
| `ProductionBoard.jsx:203` | «0 позиций» и пустота |
| `OrdersScreen.jsx:230` | заголовок и тулбар, больше ничего |
| `OrderCard.jsx:42` | скелетон крутится вечно |
| `Experimental.jsx:31` | пустой раздел |

Эффект `if (!loaded) loadAll()` повторно не сработает — зависимости не изменились.
Единственный выход — F5, и то не всякий догадается.

Отдельно: `QueueSkeleton` **недостижим в принципе** — условие `dept && loading && !loaded`
не выполнимо никогда, потому что `departments` и `loaded: true` пишутся одним `set()`
(`ordersSlice.ts:62-67`), а `dept` берётся из `departments`.

**Фикс:** один блок «Не удалось загрузить · Повторить» по образцу `Warehouse.jsx:167-172`
на все семь экранов; условие скелетона очереди — `!loaded && !loadError`.

### 9. Цветовой код статусов в светлой теме не читается: «ожидает» — 2.02:1
`erp/erp.module.css:454-462` — чипы красят текст тем же токеном, что и заливку:

| чип | цвет на фоне | контраст | норма 4.5:1 (текст 11.5px) |
|---|---|---|---|
| `chipWaiting` | `#F59E0B` на `#FFF7ED` | **2.02:1** | провал |
| `chipCyan` | `#06B6D4` на `#ECFEFF` | **2.33:1** | провал |
| `chipReady` / `chipDone` | `#10A34A` на `#ECFDF3` | **3.13:1** | провал |
| `chipBlocked` / `chipDanger` | `#EF4444` на `#FEF2F2` | **3.44:1** | провал |
| `chipViolet` | `#8B5CF6` на `#F5F3FF` | **3.86:1** | провал |
| `chipProgress` | `#2563EB` на `#EFF6FF` | 4.75:1 | ок |

Плюс просрочка вне чипа (`:687-688`): `.dueSoon` = **2.15:1**, `.overdue` = **3.76:1** на белой
карточке — а `.overdue` встречается 36 раз. И `--text-muted: #9CA3AF` внутри `.shell` даёт
**2.54:1** на карточке и **2.31:1** на `--surface` — им набраны подписи позиций, заголовки
групп меню (10px!), «не закреплено», подсказки.

Цвет статуса — единственный быстрый сигнал для цеха. На планшете при цеховом освещении
«ожидает» читается как серый шум. Ирония: **тёмная тема этих проблем не имеет**
(`chipWaiting` 7.33:1, `chipBlocked` 6.27:1) — сломана именно светлая, дефолтная.

**Фикс:** в светлом блоке `.shell` завести отдельные «чернильные» оттенки для текста,
оставив `--color-*` для заливок, рамок и точек. Проверенные значения:
`--color-warning-ink: #B45309` (4.73:1), `--color-success-ink: #067647` (5.40:1),
`--color-error-ink: #B42318` (6.01:1), `--color-cyan-ink: #0E7490` (5.15:1),
`--color-violet-ink: #6D28D9` (6.48:1). `--text-muted` → `#6B7280` (4.83:1),
в тёмной — `#9AA3B3` (6.78:1).

---

## P1 — заметно мешает (31)

### Производство и права
1. **Смена статуса перетаскиванием в канбане не проверяет права** (`ErpKanban.jsx:139-143`).
   Соседние жесты гейтятся, этот — нет. Работник швейки закрывает этап ДТФ с доски.
2. **Возврат брака откатывает промежуточные этапы, а рабочий видит только целевой**
   (`StageActionsPanel.jsx:296-300` → `stagesSlice.ts:154-171`). В выпадашке «Вернуть: Швейка»,
   по факту переоткроются ещё ВТО и Печать со снятым `qty_done`.
3. **На планшете нельзя менять приоритет, но интерфейс обещает, что можно**:
   `DepartmentQueue` не вызывает `useTouchDndPolyfill` (он только в `ErpKanban.jsx:65`),
   а `QueueRow.jsx:52` показывает ручку и подсказку «Перетащите, чтобы изменить приоритет».
4. **Колонку канбана нельзя проскроллить во время перетаскивания** — автоскролл только
   горизонтальный (`ErpKanban.jsx:95-102`), при `touch-action: none` и `max-height` колонки.
   Поставить задание на приоритетное место ниже видимой части невозможно ни мышью, ни пальцем.
5. **Выбранный цех переживает выход из системы**: `erp_my_dept` пишется напрямую в
   `localStorage` (`DepartmentQueue.jsx:114`) и не входит в `APP_KEYS` (`lib/storage.ts:35-45`).
   На общем цеховом планшете следующий рабочий попадает в чужой цех — и, поскольку
   `canActInDept` без привязки возвращает `true`, с полным набором кнопок.
6. **Счётчик просрочек на вкладке цеха нечитаем**: инлайн-стиль меняет только фон,
   `color` остаётся `--text-mid` — серый на красном, контраст ≈2:1 при кегле 11px
   (`DepartmentQueue.jsx:290`, ср. корректный `.deptTabHot` в `erp.module.css:673-677`).
7. **Кнопка «← В очередь цеха» теряет фильтры и позицию прокрутки** — ссылка собирается
   без `search` (`ProductionTask.jsx:133`), а `useScrollRestore` ключуется по `pathname+search`.
8. **Обрезанные названия невозможно прочитать**: `title` у ссылки дублирует номер вместо
   полного названия, у подстроки `title` нет вовсе (`QueueRow.jsx:60-72`, `QueueCard.jsx:57`).

### Доска, дашборд, навигация
9. **Канбан недоступен с клавиатуры** (`kanban/KanbanCard.jsx:38-53`): нет `tabIndex`,
   `onKeyDown`, `role="button"`. Канбан Order Studio это умеет — есть с чего копировать.
   Плюс `role="list"` на доске и `role="listitem"` на карточках через два промежуточных
   элемента: связь для скринридера разорвана.
10. **У канбан-вида нет пустого состояния** — заглушка привязана к `view === 'table'`
    (`ProductionBoard.jsx:205`). Пустая область без единого слова.
11. **Счётчик «N позиций» не относится к тому, что на экране** (`ProductionBoard.jsx:191`):
    в канбане набор совсем другой, а счётчик остаётся от таблицы.
12. **Из производственного плана нельзя открыть заказ**: `<td>{order.bitrix_id || '—'}</td>` —
    обычный текст (`:233`), ни `Link`, ни `orderLinkClick`, ни `useOrderDrawer` в файле нет.
13. **Свёрнутый сайдбар прячет все счётчики заданий** (`erp.module.css:318-322`), а на
    планшете он свёрнут по умолчанию (`ErpLayout.jsx:40-44`). Меню цехов превращается
    в столбик эмодзи без цифр; `title` тоже без счётчика.
14. **Колокол и «Мой цех» всегда 0 у диспетчера и директора** (`ErpLayout.jsx:80,89-92`):
    без привязки к цеху `myCode === ''`. Единственный глобальный индикатор «что горит»
    мёртв именно для тех, кому адресован.
15. **Уведомления на дашборде — тупик** (`ErpDashboard.jsx:304-311`): «Просрочен заказ №1042»
    не ссылка, хотя `order.id` в замыкании есть, а дедлайны рядом — ссылки.
16. **Таблица дашборда уезжает вправо**, колонка СТАТУС не видна, подсказки прокрутки нет
    (`ErpDashboard.jsx:220`); готовый `useScrollHints` применён только к вкладкам очереди.

### Заказы и форма создания
17. **Черновик обещает сохранить всё, но молча теряет ТЗ-PDF и превью**
    (`CreateOrderModal.jsx:210-214` — текст диалога; `:54,99` — файлы в черновик не пишутся).
    Менеджер закрывает форму, доверившись тексту; при возврате поля на месте, три PDF исчезли,
    и заказ не создаётся, пока их не найти заново.
18. **Строка позиции разъезжается на «Подряде»**: 7-колоночная grid-сетка
    (`erp.module.css:537-546`) получает 9-12 детей (`create/ItemBlock.jsx:101-183`).
    Проверено рендером: подряд + отдельная операция + доработка = 12 детей, свободный ввод
    «Операция подрядчика» сжимается в колонку 32px.
19. **ТЗ назначается вручную каждому цеху** (`create/TzSection.jsx:74-97`): нет «назначить всем»
    и автовыбора единственного файла. «Пошив + ДТФ» на две позиции = 8-10 одинаковых выборов,
    и до последнего кнопка «Создать заказ» заблокирована.
20. **Ошибки ссылаются на «поз. 2», а номеров позиций в интерфейсе нет**
    (`utils/orderForm.ts:247` vs `ItemBlock.jsx:29-30` — блок вообще без заголовка).
21. **Заполненная позиция и нанесение удаляются мгновенно, без подтверждения**
    (`ItemBlock.jsx:207-215`, `:252-255`). Позиция может содержать размерную сетку
    на несколько цветов × 7 размеров; `removeItem` заодно выбрасывает приложенные к ней ТЗ.
22. **«Выберите следующий участок» — тостом вместо инлайн-ошибки**
    (`CreateOrderModal.jsx:271-278`): поле не получает ни рамки, ни `aria-invalid`,
    ни автоскролла, в `validateOrderForm` не участвует.
23. **Черновик со вчерашней датой запуска блокирует кнопку и врёт в подсказке**
    (`utils/orderForm.ts:235-242`): «Осталось заполнить: Дата запуска» — поле заполнено,
    просто дата в прошлом. Нужно разделить «не заполнено» и «заполнено неверно».
24. **Возврат в список не восстанавливает контекст** (`OrdersScreen.jsx:45-47`):
    `useScrollRestore` не вызывается, `tab`/`dateFrom`/`dateTo` не в URL.
    В архиве вдобавок теряется всё, что догрузили кнопкой «Показать ещё».
25. **Drawer заказа не в истории браузера** (`useOrderDrawer.ts:14-18`, `ErpApp.jsx:76`):
    «Назад» уносит с экрана, а панель остаётся висеть поверх уже другого экрана.
26. **Escape в инлайн-правке закрывает всю карточку заказа**: `InlineEdit.jsx:70-73`
    не гасит событие, оно всплывает до контейнера с `useFocusTrap` (`hooks/useFocusTrap.js:41-45`).
27. **«Клиент» и «Файлы» существуют только в боковой панели** — на полной странице
    `/orders/:id`, которую как раз и шлют коллегам, их нет (`OrderCard.jsx:50-67`).
28. **Маршрут показан трижды подряд** в карточке (`orderCard/OrderItemSection.jsx:28-30`, `:89-136`):
    степпер точками → шкалы по цехам → таблица этапов. Цех и статус повторяются в трёх
    представлениях, и так на каждую позицию.

### Админка и сопровождающие циклы
29. **Смена собственной роли и самоотключение — в один клик, без подтверждения**
    (`EmployeesScreen.jsx:124-134`). Своя строка ничем не выделена. Админ меняет себе роль
    на «Дизайнер» → `/admin` закрывается мгновенно, и вернуть роль уже некому.
30. **Отключение цеха и снятие «производственный» — без подтверждения и без счётчика последствий**
    (`DepartmentsScreen.jsx:225-231`, `:176-181`). Участок с активными этапами исчезает
    из очередей, канбана и меню; снятие `is_production` выводит его ещё и из гейта ТЗ.
31. **Пустой результат поиска = пустое место** (`Subcontracting.jsx:236-240`,
    `Experimental.jsx:120-124`): если операции есть, но запрос не совпал — не рендерится
    ни таблица, ни пустое состояние. Ни объяснения, ни кнопки «Сбросить».
32. **Счётчики на складе не сходятся со списком**: `counts` считаются по `allRows`
    (со всеми закрытыми), а список по умолчанию фильтрует `onlyOpen`
    (`Warehouse.jsx:91-95` vs `:72,101`). Плитка «Упаковка/отгрузка 14» над таблицей из 3 строк.
    Плюс плитки — `<div>` без `onClick`, тогда как в закупке они кнопки-фильтры.
33. **Модалки закупки без focus-trap и Escape** (`FabricPurchasing.jsx:83`,
    `purchasing/SupplierOptionsModal.jsx:78`): Tab уводит фокус под оверлей, закрыть — только мышью.
34. **Длинные названия распирают таблицы** (`FabricPurchasing.jsx:347`, `Warehouse.jsx:188`,
    `Subcontracting.jsx:257`): `.subText` задаёт только цвет и размер, `max-width`/`line-clamp` нет.
35. **Приёмка материала по умолчанию «Принято полностью»** (`warehouse/MaterialReceiptCard.jsx:39`):
    недостача 40 кг уезжает как полная приёмка, гейт закроя открывается, расхождение всплывает в цехе.

### Дизайн-система и доступность
36. **Тач-таргеты: `@media (pointer: coarse)` покрывает 11 классов из ~20 интерактивных**
    (`erp.module.css:1591-1603`). Не покрыты и меньше 44px: `.chip` ≈25px (**14 кнопок**,
    включая три быстрых фильтра цеха, чипы этапов и переключатель «Таблица/Канбан»),
    `.pageBtn` 32×32 (Pagination во всех таблицах), `.drawerClose` 34×34, `.collapseBtn` 36px,
    `.iconBtn` 38×38 (кнопки шапки), `.navLink` 38px (**всё меню сайдбара**),
    `.drawerTab` ≈40px, `.dragHandle` (размер не задан вовсе, ~13px — именно за него
    бригадир таскает приоритет), `.inlineEditBtn`, `.supplierCell`. `.checkLabel` там же
    ограничен 36px вместо 44. Правило `DESIGN.md:73` существует, но список классов
    не поспевал за компонентами редизайна.
37. **9 инлайн-стилей отменяют правило `pointer: coarse`** — инлайн всегда выигрывает
    у медиазапроса: `PlanCell.jsx:34,36` (`minHeight: 30` на полях плановых дат),
    `InlineEdit.jsx:64` (30), `EmployeesScreen.jsx:126,139,156,218,232` (32, ×5).
    Плюс `QueueFilters.jsx:38` — `font: inherit` шорткатом сбрасывает `font-size: 11.5px`
    и `font-weight: 600` из `.chip`, из-за чего фильтр-чипы типографически не совпадают
    со статус-чипами рядом.
38. **`--border` не переопределён в `.shell`** (`erp.module.css:13-43` задаёт `--border-light`
    и `--border-mid`, но не `--border`) → в светлой ERP-теме он остаётся `#0A0A0A` из
    `index.css:27`. Итог: 29 мест с **чёрной** рамкой (`.card`, `.tableWrap`, `.table th`,
    `.input`, `.kanbanCol`, `.kanbanCard`, `.queueCard`, `.orderCardM`…) против 37 мест
    со светло-серой. На одном экране рядом стоят карточки редизайна и «швейцарские» блоки
    Order Studio — визуально две разные системы.
39. **Компоненты ERP вне `.shell` теряют 6 токенов.** `--bg-info`, `--bg-violet`, `--bg-cyan`,
    `--color-violet`, `--color-cyan`, `--shadow-card` объявлены только в `.shell`
    (проверено: в `index.css` и `styles/*.css` их нет). `AdminScreen` смонтирован в оба
    раздела, причём в Order Studio — вне `.shell` (`OrderStudioApp.jsx:145`). Конкретное
    следствие: чип «Ждёт подтверждения» (`EmployeesScreen.jsx:22` → `.chipProgress` →
    `background: var(--bg-info)`) теряет плашку и рендерится голым синим текстом.
40. **Поле глобального поиска — единственный элемент с погашенным фокусом**:
    `.headerSearch input { outline: none }` (`erp.module.css:119-127`) без компенсирующего
    `:focus-within`. Это первый по Tab элемент после сайдбара — фокус «исчезает».
41. **В ERP нет skip-link, хотя якорь уже есть**: `ErpLayout.jsx:186` рендерит
    `id="main-content"`, а ссылки на него нет; в Order Studio она есть
    (`OrderStudioApp.jsx:116`), и стиль `.skipLink` готов. Сайдбар — до 20+ ссылок,
    и клавиатурный пользователь проходит их заново на каждой странице.
42. **Тёмная тема: два цвета из несуществующих токенов.** `.tzAssignMissing` использует
    `var(--danger, #c0392b)` — токена `--danger` нет нигде, всегда работает фолбэк,
    и в тёмной теме это **3.17:1** вместо 6.23:1, которые дал бы `--color-error`.
    А `DESIGN.md:60` называет «не назначено» единственным красным блока ТЗ — самым важным
    сигналом. Там же `font-family: var(--font-mono, monospace)` (`:976`) — токена нет,
    есть `--mono`; версия ТЗ рендерится системным моноширинным вместо Roboto Mono.
    И активная вкладка цеха в тёмной теме — **3.00:1** (`--white` не переопределён в `.shell`).
43. **Перетаскивание в ERP не имеет клавиатурной альтернативы** — регресс относительно
    Order Studio, где `components/orders/KanbanCard.jsx` умеет `tabIndex={0}` + стрелки.
    Приоритет задания в очереди (`reorderStageQueue`) с клавиатуры не выполним вообще:
    во всём `erp/` `tabIndex` встречается один раз.
44. **6 tablist без tabpanel, `aria-controls` и стрелок**: `AdminScreen:38`, `Drawer:32`
    (ещё и без `aria-label`), `DepartmentQueue:270`, `ProductionBoard:162`, `OrdersScreen:131`,
    `DictionariesTab:195`. `role="tabpanel"` — **0** вхождений во всём `erp/`.
    Паттерн объявлен наполовину: скринридер обещает «вкладка 3 из 5», панель не связана,
    Left/Right не работают, каждая вкладка — отдельная остановка Tab (в `Drawer` их 7).
45. **8 полей ввода помечены только placeholder'ом** (из 126): в том числе
    «Что мешает?» и «Причина брака» (`StageActionsPanel.jsx:240,283`) — ключевой ввод цеха.
    Placeholder исчезает при вводе (WCAG 3.3.2).

---

## P2 — раздражает, но работать не мешает

**Загрузка и пустые состояния.** «Загрузка…» текстом вместо скелетона —
`FabricPurchasing.jsx:314`, `ProductionTask.jsx:87` · `QueueSkeleton` рисует карточки там,
где будут строки (`ErpSkeletons.jsx:63-84`) · скелетон дашборда — 4 плитки старого класса
вместо 6 новых, лейаут заметно перестраивается · в канбан-виде показывается `TableSkeleton` ·
пустые группы очереди скрываются целиком, и рабочий не понимает: работы нет или блок не загрузился ·
«Справочник пуст» при скрытых значениях (`DictionariesTab.jsx:97-99`) · «Пользователей не видно —
нужны права администратора» на экране, который открыт только админам (`EmployeesScreen.jsx:102-104`).

**Доступность.** `role="tablist"` без `tabpanel`, `aria-controls` и стрелочной навигации —
`AdminScreen.jsx:38-51`, `DictionariesTab.jsx:195-217`, `DepartmentQueue.jsx:270-296`,
`OrdersScreen.jsx:131-183` (там же в tablist лежат не-табы) · Lightbox без focus-trap
(`queue/Lightbox.jsx`), хотя `TzViewer` рядом использует `useFocusTrap` · кнопки без подписей:
`PlanCell.jsx:45-46` (✓/✕), `EmployeesScreen.jsx:255-258` (↩), `SupplierOptionsModal.jsx:171-173` (✕) ·
`InlineEdit` перекрывает значение своим `aria-label` · строка заказа раскрывается только мышью
(`OrderRow.jsx:29-33`) · эмодзи-индикаторы без `role="img"` (`QueueRow.jsx:91-100`).

**Числа и подписи.** «1 заданий» / «1 позиций» — `pluralize` есть, не используется
(`DepartmentQueue.jsx:308`, `ProductionBoard.jsx:191`) · один цех — два разных числа
на одном экране (бейдж колонки канбана считает завершённые, бейдж сайдбара — нет) ·
«70/250 шт» в карточке: знаменатель — тираж × число этапов, а подпись читается как «250 изделий» ·
«Открыть заказ №—» у заказов без сделки Bitrix (`OrderDrawer.jsx:78-80`) ·
история молча обрезана до 120 событий без пометки (`HistorySection.jsx:73`).

**Раскладка.** `.shell` на `100vh` вместо `100dvh` (в том же файле для модалок уже `100dvh`) ·
сайдбар без единого медиазапроса: на 480px он либо 64px постоянно, либо 236px — половина
экрана, без оверлея · доска до 6 колонок ≈1810px без подсказок горизонтального скролла ·
на 761-1100px из строки очереди первым исчезает прогресс — главный показатель ·
двойной ряд одинаковых фильтров в закупке и на складе (KPI-плитки + чипы-вкладки
с теми же названиями и счётчиками) · на мобильном фильтры занимают весь первый экран ·
на экране очереди два визуально одинаковых поля поиска подряд · кнопки формы создания
закреплены только на мобильном (`erp.module.css:594` vs `:1164-1174`).

**Формы.** Нативные `type="date"` в русском интерфейсе показывают `mm/dd/yyyy` при en-US
локали браузера — 10 мест · форма брака: 8 полей без подписей, только плейсхолдеры
(`StageActionsPanel.jsx:265-393`) · чипы справочников молча затирают набранный текст ·
чекбокс «Нужен новый материал» серый и снят, когда поля материала уже показаны ·
нет busy-состояния ни у одного действия цеха — на медленном Wi-Fi нет обратной связи,
что тап засчитан · двойная отправка комментария (`CommentsSection.jsx:29-48`).

**Долг дизайн-системы.** Токены почти не используются: 89 `font-size` литералом против
14 через `var(--type-*)`, 201 отступ px-литералом против 6 через `var(--space*)` — то есть
86% кеглей и 97% отступов магические, при объявленной и неиспользуемой шкале
(21 разное значение `font-size` вместо семи ступеней) · 23 правила с текстом меньше 12px
при собственном правиле «≥12px», причём сама шкала это санкционирует
(`--type-label: 11px`, `--type-caption: 9px`); худшее — `.fieldLabel` 10px, а это ярлык
каждого поля ERP, 49 употреблений · фолбэки `var(--token, X)` разошлись с реальными
значениями (`--radius-md` объявлен 8px, в ERP фолбэк 10px ×21; `--z-modal` объявлен 600,
фолбэк 1000) — тихий второй источник правды · четыре полноэкранных слоя резолвятся
в один `z-index: 600`, порядок наложения держится только на порядке в DOM ·
дубли примитивов: прогресс-бар реализован трижды (две реализации совпадают буквально,
`.progressTrack` и `.routeBar`), пустое состояние дважды (`.stub`/`.emptyState`,
разница 2px паддинга), миниатюра дважды, индикатор стадий — тремя независимыми системами ·
11 мёртвых классов, включая всю старую KPI-систему (`.kpiValue/.kpiLabel/.kpiWarn/…`,
38 строк), из которой жив только `.kpiTile` — в скелетоне, из-за чего он и не совпадает
с контентом.

**Прочее.** Удаление заказа «✕» в постоянной колонке рядом с «Отгрузить» ·
красный чип не на проблеме, а на атрибуте («Без Честного знака») ·
«просрочен 5 дн.» рисуется серым из-за вложенного `.subText` (`DueCell.jsx:17-20`) ·
подряд: «Следующее действие» дублирует «Текущий статус» на половине строк ·
справочник: ↑/↓ перепрыгивает через скрытые значения (`DictionariesTab.jsx:118-135`) ·
сотрудника можно привязать к отключённому цеху · мёртвый `datalist` и мёртвые импорты
после выноса `ItemBlock` · пустая дорожка канбана обозначена прочерком «—»
вместо «Перетащите сюда» · матрица прав без липкой первой колонки и без различения
«явно задано» / «дефолт» · таблицы без липкой шапки и сортировки.

---

## План устранения

Шесть волн, по убыванию вреда. Каждая — отдельный коммит, зелёные `lint` + `test` + `build`
обязательны, e2e-гейт CI уже стоит. UX-1 и UX-2 закрывают все девять P0, UX-6 —
чисто технический долг и делается только по желанию.

**Одно решение за заказчиком (блокирует пункт 3 волны UX-1):** матрица прав сейчас врёт
про руководство (P0 №5) и не работает на 7 правах из 10 (P0 №6). Два пути:
**(а)** сделать матрицу настоящей — провести все права до кнопок, а колонку «Директор»
закрыть замком, чтобы админ не мог отключить себя; **(б)** оставить матрицу как есть,
но убрать из неё нереализованные строки и ложную подпись. (а) — честнее и дороже,
(б) — полдня и без сюрпризов. По умолчанию беру (а): матрица уже заявлена в интерфейсе
как инструмент управления, и оставлять её декоративной хуже, чем доделать.

### UX-1. Данные и права (P0 · ~день)
Всё, что портит данные или врёт про доступ.

1. Порядок drop-событий в канбане + компонентный тест на три жеста.
2. Гейт и подтверждение на закрытии этапа: чип производственного плана (`onAdvance`),
   `useStageActions.onDone`, дорожка «Завершено» в канбане — три точки, один
   `confirm()`-хелпер с расчётом «останется незакрытыми N шт».
3. Права: провести `can('stage.take'|'stage.progress'|'stage.complete'|'stage.block'|'stage.defect')`
   в `StageActionsPanel`/`useStageActions`, `catalog.edit` — в админские вкладки,
   `ProductionBoard` подключить к `useErpAccess`. Решить с заказчиком: делать
   `can = isPrivileged || …` или снять ложную подпись в матрице.
4. `canActInDept` без привязки → сузить (сейчас `true` для всех цехов).
5. Фильтры производственного плана: перевести таблицу на `applyStageFilters`.
6. Повторная отправка формы создания: новый `groupId` на попытку.

### UX-2. Ошибки, загрузка, пустота (P0 №8 + P1 · ~полдня)
Один общий компонент `<LoadFailed onRetry />` и один `<EmptyResult query onReset />`,
затем 7 экранов на первый и 2 на второй. Заодно чинится недостижимый `QueueSkeleton`
и текстовые «Загрузка…».

### UX-3. Цех на планшете: контраст и тач (P0 №9 + P1 · ~день)
Самая механическая волна — почти вся в `erp.module.css`, риск регрессий минимальный.

1. **Чернильные токены** `--color-*-ink` в обоих блоках `.shell`, перевод на них `color:`
   всех шести чипов и `.dueSoon`/`.overdue`; `--text-muted` → `#6B7280` / `#9AA3B3`.
2. **Тач-таргеты**: дописать в блок `pointer: coarse` `.chip`, `.pageBtn`, `.drawerClose`,
   `.drawerTab`, `.collapseBtn`, `.iconBtn`, `.navLink`, `.dragHandle`, `.inlineEditBtn`,
   `.supplierCell`; `.checkLabel` 36 → 44. Девять инлайн-`minHeight` заменить классами
   `.inputXs`/`.chipBtn` и включить их туда же; `font: inherit` → `font-family: inherit`.
3. **Несуществующие токены**: `var(--danger, …)` → `var(--color-error)`,
   `var(--font-mono, …)` → `var(--mono)`, `var(--white, #fff)` → `var(--btn-primary-fg)`.
4. **`--border`** объявить в обоих блоках `.shell` (`#E5E7EB` / `#242B39`) — 29 чёрных
   рамок разом становятся серыми, ERP перестаёт выглядеть как две системы.
   Прогнать визуально: доска, очередь, заказы, админка, светлая и тёмная.
5. Шесть недостающих токенов (`--bg-info`, `--bg-violet`, `--bg-cyan`, `--color-violet`,
   `--color-cyan`, `--shadow-card`) — в `:root` и `html[data-theme="dark"]`.
6. `useTouchDndPolyfill` в очереди (или кнопки ↑/↓ на coarse) · вертикальный автоскролл
   колонки канбана · `erp_my_dept` в `APP_KEYS` · `100dvh` · сайдбар-оверлей на <760px ·
   счётчики в свёрнутом сайдбаре · мобильное представление производственного плана
   (карточки, как `OrderCardMobile`).

### UX-4. Форма создания и карточка заказа (P1 · ~день)
Grid-сетка позиции под подряд · заголовок «Позиция N» · «Назначить ТЗ всем цехам» +
автоподстановка единственного файла · честный текст черновика про файлы · `confirm()` на
удаление непустой позиции/нанесения · `return_dept` в `validateOrderForm` · разделение
`missing`/`invalid` · `stopPropagation` на Escape в `InlineEdit` · Drawer в URL (`?order=`) ·
`useScrollRestore` + фильтры в URL на экране заказов · «Клиент» и «Файлы» на полной странице ·
маршрут в карточке — одно представление вместо трёх.

### UX-5. Навигация, админка, доступность (P1 хвост + P2 · ~день)
Кликабельные номера заказов на производственном плане и в уведомлениях дашборда ·
пустое состояние канбана и честный счётчик · глобальные счётчики для ролей без цеха ·
`confirm()` на смену своей роли и отключение цеха (с числом незакрытых этапов) ·
KPI склада: кнопки-фильтры + счётчики по видимому набору · `line-clamp` + `title` в таблицах ·
`pluralize` · липкие шапки таблиц и первая колонка матрицы прав.

**Доступность одним блоком:** skip-link в `ErpLayout` (компонент готов в Order Studio) ·
`:focus-within` на `.headerSearch` вместо `outline: none` · `useFocusTrap` + Escape
в двух модалках закупки и в `Lightbox` · клавиатура в канбане (`tabIndex` + стрелки,
копия из Order Studio) и кнопки «выше/ниже» в очереди · `aria-label` четырём кнопкам-глифам
(`SupplierOptionsModal:171`, `PlanCell:38,45`, `EmployeesScreen:256`) · восемь полей
обернуть в `.field`/`.fieldLabel` · шесть `role="tab*"` либо довести до WAI-ARIA
общим компонентом, либо честно снять роль и оставить кнопки — второе дешевле и ничего
не ломает, кроме `Drawer`, где вкладки настоящие.

### UX-6. Долг дизайн-системы (P2 · отдельно, по желанию)
Не блокирует ничего, но каждая следующая правка обходится дороже без этого:
удалить 11 мёртвых классов и старую KPI-систему (скелетон перевести на `.kpiCard`) ·
слить дубли примитивов (прогресс-бар ×3 → 1, пустое состояние ×2 → 1, миниатюра ×2 → 1) ·
убрать фолбэки `var(--token, X)` — токены определены в `index.css`, импортируемом первым ·
развести z-index (`--z-drawer: 600`, `--z-modal: 700`, `--z-lightbox: 800`) ·
поднять `--type-label` до 12px и перевести на шкалу хотя бы самые частые кегли.

**Не входит в план (решать отдельно):** замена нативных `type="date"` на свой компонент —
это новая зависимость или свой календарь, обсуждается отдельно; сортировка таблиц закупки
и склада — фича, а не дефект; сведение трёх систем индикаторов стадий
(`.stepper*` / `.numStepper*` / `.pipe*`) в одну — задокументировано в `DESIGN.md`
как три паттерна, менять только с согласия заказчика.

---

## Что сделано хорошо — не сломать при правках

- **Единый источник группировки и фильтров** (`buildQueueEntries` + `applyStageFilters`):
  очередь, канбан и план считают группы и причины ожидания одинаково, фильтры живут в URL.
- **Один набор действий на три поверхности** (`StageActionsPanel` + `useStageActions`) —
  строка очереди, мобильная карточка и страница задания не расходятся.
- **Серверная валидация количеств**: `reportProgress` клампит к тиражу и честно предупреждает,
  `reportDefect` не даёт браку превысить ни тираж, ни фактически сделанное.
- **Инлайн-валидация формы создания реализована целиком**: `aria-invalid` + `aria-describedby` +
  автоскролл к первому ошибочному с раскрытием секции + «Осталось заполнить: …».
- **Гейт ТЗ объясняет себя**: «для позиции «X» не назначено ТЗ: Раскрой, Швейка» вместо
  абстрактного «заполните обязательные поля».
- **Транзакционность создания заказа продумана и задокументирована**, включая честный
  комментарий про цену решения (файлы-сироты).
- **Опасные экраны объясняют правила словами**: почему участки не удаляются, почему значения
  справочника отключаются, что делает выбранный вариант поставщика.
- **Четыре различимых состояния зоны приёма в канбане** + маркер места вставки —
  заметно лучше среднего канбана.
- **Перетаскивание не возит страницу**: `overscroll-behavior`, `touch-action` только на время
  drag, автоскролл доски вместо окна, полифилл тач-DnD лениво и только на `pointer: coarse`.
- **Красный дисциплинирован**: точка дедлайна, полоса заблокированной карточки, чип срока —
  строка целиком не красится. `DueCell` корректно снимает «просрочен» с отгруженных.
- **`orderLinkClick`**: ЛКМ → Drawer, Ctrl/Cmd/Shift → нормальный диплинк, `stopPropagation`,
  чтобы клик по названию не раскрывал строку.
- **Optimistic + rollback + toast** во всех мутациях, без исключений.
- **KPI закупки — эталон**: кликабельны целиком, фильтруют список, сбрасываются повторным
  кликом, одна найденная позиция открывает карточку сразу.
- **`prefers-reduced-motion` уважается глобально и корректно** (`styles/utils.css:356-362`),
  единственное `!important` в проекте документировано ссылкой на WCAG 2.1 SC 2.3.3.
  В самом `erp.module.css` — ноль `!important`.
- **Инлайн-стили не содержат цветов**: из 114 объектов `style={{…}}` ноль захардкоженных
  `#hex`/`rgb()`, ноль `fontSize`. Тёмная тема от инлайнов не ломается нигде.
- **Тёмная тема — не пост-фактум**: полный второй набор из 30 токенов, подобранных вручную,
  и по контрасту он лучше светлого (chipWaiting 7.33:1 против 2.02:1).
- **Изображения и iframe размечены полностью**: все 8 `<img>` с осмысленным `alt`,
  оба `<iframe>` с `title`, заглушки битых превью — `aria-hidden`.
- **`useFocusTrap` — качественная реализация**: восстанавливает предыдущий фокус,
  пересчитывает список фокусируемых на каждый Tab, держит `onEscape` в ref.
- **Скелетоны объявлены как живые области** — `role="status"` + осмысленный `aria-label`.
- **Правило «мобильные модалки» доведено до конца**: `100dvh`, sticky-кнопки, чтобы
  отправка не уезжала за клавиатуру.
