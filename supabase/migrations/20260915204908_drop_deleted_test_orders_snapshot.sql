-- Снимок удалённых тестовых заказов (чистка 13.09) — удаляется по решению
-- владельца 15.09: откат больше не нужен.
--
-- ПОЧЕМУ ЭТО НЕ ПРОСТО УБОРКА. Таблица заведена через `create table as` и
-- унаследовала права по умолчанию: RLS выключен, политик ноль,
-- `has_table_privilege('anon', …, 'SELECT')` — true. Ключ `anon` публичен
-- по построению (лежит в бандле фронтенда), то есть снимок двадцати
-- удалённых заказов вместе с зависимыми строками в jsonb читался через
-- PostgREST любым, кто открыл сайт. Адвизор Supabase помечал это уровнем
-- ERROR (`rls_disabled_in_public`).
--
-- Проверено перед удалением: 20 строк, 160 кБ, внешних ключей на таблицу
-- нет, представлений, читающих её, нет.
drop table if exists public.erp_deleted_test_orders_20260913;
